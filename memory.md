# Project memory

Running log of project-level context that should persist across sessions and agent invocations. Append new entries with a date; don't rewrite history.

## 2026-07-04
- Repo created: https://github.com/pr4ject4ne-code/pr4ject4ne (public).
- Agent fleet set up in `.claude/agents/`: planner, builder, code-reviewer, test-runner, debugger, docs-writer. All inherit the orchestrating session's model (no pinned `model:` field) so they scale with whatever is driving the session. See [CLAUDE.md](CLAUDE.md) for the intended pipeline.
- Evaluated and declined "ruflo" (third-party multi-agent orchestration npm package, formerly Claude Flow) due to supply-chain/cross-machine "federation" concerns — built this project's own lightweight agent fleet instead.
- Standing policy: all changes are committed and pushed to `origin` automatically without asking for confirmation each time.
- Agent system upgraded: added `scout` (haiku-pinned read-only recon agent); planner now emits a structured handoff format with per-phase verification commands and parallelizable-step markers; builder auto-commits/pushes per git policy; test-runner + code-reviewer declared parallel; CLAUDE.md gained orchestrator efficiency rules (delegate searches to scout, fan out independent work, skip needless stages).
- Founder docs ingested: 26 PDFs ("Ihn.zip") digested by parallel agents into docs/research/ (one summary per source doc). Product defined: **AWANG — Integrated Hospital Network**; SPEC.md written (MVP = clinic workflow: booking, queue, reminders, doctor's window; biodata farm/security keys/analytics deferred to later phases). Repo flipped to **private** because spec contains pricing, named hospital prospects, and partner strategy.
- Key facts: market = Nigerian private clinics starting Enugu; NDPC/NDPA compliance on critical path; pricing tiers ₦15k–₦150k+/mo + 500-patient volume blocks (per-visit fees rejected); founder is a medical student with an anchor professor/CMD partner; "Ruflo" in the founder's Setup doc is the tool the user earlier asked to install (declined for supply-chain concerns; own agent fleet substitutes).
- Scope reset by founder: the 26 docs are thought-context only, "nothing there is solid". Actual build = **Racoon Eye v1**: (1) hospital search & info with hospital self-registration (+ seeded Enugu draft listings), (2) patient data save (account + two-layer biodata profile, clinical fields unverified for now), (3) first aid section as interactive triage (needs clinical review + disclaimer). Responsive web app; TS/Next.js + PostgreSQL modular monolith as working default. SPEC.md updated accordingly.
- **Session handoff / next step:** a planner run for the Racoon Eye v1 implementation plan was in flight when the session ended — its output was not captured. Next session: re-run the planner against SPEC.md "Current build: Racoon Eye (v1)" (Phase 0 = scaffold + CI, then the three modules), save the plan to PLAN.md, then fan out builders. No application code exists yet.
- Production-readiness pass: added `security-auditor` (systematic OWASP-style audits, mandatory for changes touching auth/input/secrets) and `devops` (CI/CD, containers, config/secrets, observability, release safety). CLAUDE.md now has a production readiness checklist (security, testing, CI/CD, config, error handling, observability, data safety, performance, accessibility, docs) with named agent owners. When first code lands, devops should set up GitHub Actions CI enforcing the automatable items.

## 2026-07-05
- Racoon Eye needs both a website and an app; app will run off the same server/API, so **website first, app later** (reusing the backend). Confirmed with founder.
- New convention: **`docs/pages/` holds one spec file per page/screen** (homepage.md, login.md, first-aid.md, dashboard.md so far), freely readable by all agents but primarily consumed by planner so it can fully grasp each page before producing an implementation plan. Add a new file here whenever a new page is discussed; update the relevant file the moment page details are confirmed rather than leaving it only in chat.
- [homepage.md](docs/pages/homepage.md) fully discussed and confirmed: matte blue/white/amethyst-purple theme (no gradients into white, no blue/purple text directly on white); minimal round-edged raccoon-face logo (nose, eye, eye-cutout only); top bar = logo left, 3-mode search bar (nearest hospital / specific hospital / symptom search→recommendation) left-of-center, hamburger menu right (Profile→login/dashboard, First Aid link); full 100vw/100vh interactive map background; expandable hospital mini-profile cards below map (click → separate full profile page, not yet designed); footer with logo right, privacy/T&Cs/link-tree links left.
- **Map stack decided: Leaflet.js + OpenStreetMap tiles + OSRM routing.** Chosen specifically because it needs zero API key/account/billing anywhere in the stack (Mapbox and OpenRouteService both require signup even free-tier, which risked a future cost conversation the founder explicitly wants to avoid). Public OSRM demo server is fine for MVP; self-hosting OSRM is the scale-up path if needed later.
- login.md, first-aid.md, dashboard.md created as stubs only (not yet discussed with founder) — planner should not assume structure for these beyond what SPEC.md already states until the corresponding doc is filled in.
- Symptom-search (homepage) and First Aid triage (first-aid page) have overlapping purpose that hasn't been reconciled yet — flagged as an open item in both docs.
- [first-aid.md](docs/pages/first-aid.md) fully confirmed: read-only developer-managed catalog (public browse/read, developers upload/edit only). Public design left to builder within theme rules, sans-serif fonts throughout. Every page includes a "suggestion tab" that emails feedback to a site support email (TBD). Developer section requires secure login (credentials TBD, will use env vars/secrets manager, not code); all dev actions logged (audit trail). Security-auditor must review developer access before build. Relationship between this catalog page and the homepage's symptom-search mode still open/TBD.
- [login.md](docs/pages/login.md) partially confirmed: email + password authentication (standard, no social/2FA in v1). Patients sign up with email/password → creates account that populates biodata. Institutional login flow (hospitals/partners) still TBD (separate screen or integrated selector).
- **Anthropometric measurements added to biodata:** height (cm), weight (kg), waist circumference (cm), chest circumference (cm), hip circumference (cm), BMI (calculated/display-only from height+weight).
- **Site email:** use pr4ject4ne@gmail.com for suggestion tabs and feedback routing. Keep secure — do not expose in client-side code to prevent scraping/phishing.
- [hospital-profile.md](docs/pages/hospital-profile.md) fully specced: top bar includes hospital logo beside brand logo; hospital-specific search; 5 photos (outside far, outside close, reception, 2 others); basic info (name, address, website, contact); times of service/visit; dynamic calendar with color-coded announcements (green=light, yellow=intermediate, red=urgent); personnel dropdown (doctors with speciality/level, patient-rateable); footer shared component.
- **Developer pages are disconnected from main site:**
  - [first-aid.md](docs/pages/first-aid.md) dev section updated: developer section is not linked from main site, only accessible from primary-dev-page. Upload form fields: picture, definition, description, process, do's/don'ts, things to look out for, implications, indication, contraindications. Content split into Procedures vs. Techniques.
  - [primary-dev-page.md](docs/pages/primary-dev-page.md) created: master admin hub for developer account management (create/revoke/reset), access type management, portal links, audit logs. Not linked from main site; requires hardened master login. Security-auditor must review.
  - [hospital-management-portal.md](docs/pages/hospital-management-portal.md) created: backend for hospital staff to edit their listings (media, info, hours, announcements, personnel). Separate login from patients. Strict data isolation per institution. All edits logged.
- [filter-page.md](docs/pages/filter-page.md) created: advanced search/filter across all health service types (hospitals, clinics, pharmacies, radiology, etc.), not just hospitals. Filters: service type, location, speciality/service, rating, availability. Results show name/type/address/contact/hours/rating with links to full profiles. Navigation placement TBD.
- [dashboard.md](docs/pages/dashboard.md) partially confirmed: main dashboard = "Recent" (left) + expandable on-page calendar overlay (plain calendar for v1, no queue/booking logic yet). Second section = Profile tab = the **Biodata Farm**, founder-stated as one of the product's key trust drivers. Two layers: (1) Profile — freely available, compulsory full name/alias/gender/phone/email/DOB(visibility optional)/next-of-kin, optional address; (2) Biodata — locked behind a changeable, account-holder-chosen `IHN-` prefixed access code, patient-filled (chronic disease, height, weight, occupation, marital/religious status) + official-document-recommended (genotype, blood group, timestamped clinical condition w/ free-text cause, disability, health preferences). For v1, anyone can fill these fields if they have authoritative source docs; the "recommended" means getting them from official records (lab reports, medical records) is preferable but not enforced. Clinical fields stay unverified until doctor-verification exists (later phase). Top bar/footer confirmed as shared components reused unchanged across every page, not just homepage.
- `IHN-` access code needs security-auditor attention when built (generation, rotation, rate-limiting, access logging) since it's a shareable credential to sensitive biodata.

## 2026-07-05 (continued)
- **PLAN.md written and approved** (2026-07-05). Full 6-phase implementation plan for Racoon Eye v1:
  - Phase 0: Scaffold + CI/CD (foundation)
  - Phase 1: Core backend (auth, data models, APIs)
  - Phase 2: Public frontend (homepage, hospital profile, filter) — 3 builders can parallelize
  - Phase 3: Patient dashboard (Biodata Farm + login)
  - Phase 4: First Aid (public catalog + developer section, disconnected from main site)
  - Phase 5: Hospital management portal (for verified/institutional hospitals)
  - Phase 6: Testing, security audit, launch prep (final gate)
  - Estimated 10–12 weeks for 2–3 builders + QA/test-runner + security-auditor.
- **Final decisions locked in:**
  - Symptom search: part of Phase 2 homepage search bar (3 modes: nearest, by name, by symptoms); join with First Aid triage later, not v1.
  - IHN code: static (never rotates), shareable with close relatives/friends for emergency access. Explained in UI + Terms.
  - Hospital data tiers: Verified (institution account, self-managed via Phase 5 portal) + Community-managed (dev-seeded, improved via suggestions dashboard). Distinction flagged on hospital-profile page.
  - Suggestions workflow: simple & informational. Suggestions dashboard in dev portal (/dev/primary); devs manually review and update community-managed hospital data.
  - Image storage: deferred (use local FS for dev; S3/server TBD when infrastructure ready).
  - First-aid disclaimer: "Educational reference only, not legal medical advice, always back-check with professionals. Not provided under law." Added to Terms & Conditions, displayed on all first-aid pages.
  - Hospital management portal: included in Phase 5 for verified hospitals to self-edit listings.
  - Developer pages: disconnected from main site (no links from header/footer/homepage), only accessible via direct URL or /dev/primary hub.
  - Legal/compliance: NDPC/NDPA review deferred to post-launch (parallel workstream, doesn't block v1 launch).
  - Doctor roster: dropdown with specialty filter + alphabetical sort.
  - Page specs: all 8 pages locked in docs/pages/ (homepage, login, dashboard, hospital-profile, first-aid, hospital-management-portal, primary-dev-page, filter-page); this is the planner's source of truth.
  - Anthropometric measurements confirmed in biodata: height, weight, waist/chest/hip circumference, BMI (calculated).
  - Site email: pr4ject4ne@gmail.com for suggestion tabs (kept secure in backend, no client-side exposure).
  - Top bar/footer: shared components consistent across all pages.
  - Theme: matte blue/white/amethyst, sans-serif fonts, no gradients into white, no blue/amethyst text directly on white.
- Builders should now pull PLAN.md, read the corresponding page specs in docs/pages/, and start Phase 0 (scaffold). Ready for builder handoff.

## 2026-07-06
- **Racoon Eye v1 build Phases 0–5 COMPLETE, committed + pushed.** Next.js 14 (App Router) + TypeScript + PostgreSQL. Commits: 51ce51d (P0 scaffold), a45b3be (P1 backend), 89b9bdc (P2 public frontend), c95d63d (P3 login+dashboard/Biodata Farm), 75ce331 (P4 First Aid public+dev), deb082e (P5 hospital portal).
- State at end of build: **89 tests passing, lint clean, production build green** (37 routes). Builder agent was interrupted twice (connection drop mid-P2, session limit mid-P5) but resumed from its own committed work each time; orchestrator (main session) finished + verified P5 and committed it.
- Fixed a test-mock bug in hospital-isolation.test.ts: it stubbed the intra-module getHospitalStaff (which Jest can't intercept for internal calls) so the real code ran against an incomplete db mock (`queryOne is not a function`). Fix: mock the @/lib/auth boundary (getSession/findUserById) instead and exercise the REAL requireHospitalOwnership guard — stronger test. The isolation logic itself was always correct.
- Gotcha for future: running multiple `npm run build` concurrently on Windows races on `.next/` and throws a spurious `_not-found/page.js.nft.json` ENOENT during trace collection. Compile itself succeeds. Run builds one at a time.
- **Phase 6 validation DONE (agents) + remediation in progress.** Ran test-runner + code-reviewer + security-auditor in parallel. Verdict: core architecture solid (parameterized queries throughout, correct authZ/IDOR gating, bcrypt-12, hashed session tokens, pervasive audit logging, clean secrets/git history). Findings fixed (commits caea4f5, bb345d5):
  - **H1 stored-XSS** (sanitizeText only escaped `<>`, didn't validate URL scheme → `javascript:` URLs reached href/src). Fixed: added `safeHttpUrl()` in lib/sanitize, applied on every write path (hospital register/info/media, first-aid create/edit) + render guards (HospitalInfo, ResultsList).
  - **H2 Next.js advisories**: upgraded 14.2.35→15.5.20 (no fix existed in 14.x line). Required Next 15 async migration: dynamic-route `params` and `cookies()` are now Promises — awaited across all [id]/[userId] handlers + pages + auth guards; tests pass params as Promise.resolve. Remaining `npm audit`: 2 moderate transitive postcss (build-time only, needs Next 16).
  - **M1** CSP + prod HSTS added to next.config; fixed misleading middleware comment. **M2** rate-limit + sanitize public hospital registration. **M3/CR6** constantTimeEquals for IHN check. **CR1** LIKE-metacharacter escaping on search. **CR3** clamp min_rating. **CR4** announcements PATCH no-op now 400s. **L1** dropped unused SESSION_SECRET.
- **State: 89 tests passing, lint clean, Next 15 build green (37 routes).**
- **Wave 2 STILL PENDING** (delegated to builder): test-runner flagged 0% coverage on `audit.ts`, `dev-auth.ts`, all 3 login routes, plus untested biodata PATCH / hospital write-endpoint isolation / first-aid edit. Also need standalone docs (SECURITY.md, TESTING.md, DEPLOYMENT.md, CHANGELOG.md, COMPLIANCE.md) — T&C + privacy PAGES already exist in-app from P2. Lower-priority code-reviewer items deferred: CR2 (unauth list endpoints run COUNT(*) unthrottled + missing composite index), CR5 (serial queries could Promise.all), L2/L3/L4 (session sliding expiry, rate_limit_events pruning, x-forwarded-for proxy trust).

## 2026-07-07
- **Visual bar set by founder (constant from now on):** presentation is the front door — "exceptionally good and fluid, so much that it looks like that's the main aim." Target = blend of consumer polish (Linear/Airbnb/Revolut fit-and-finish) and map-first utility (Google Maps/Uber). All locked design rules (theme, placements per docs/pages/) remain constant; build around them. Founder wants blunt page-by-page critiques, not diplomacy.
- Environment note: **no Docker on this machine** — local Postgres via docker compose is unavailable; app pages render but DB-backed APIs fail until a Postgres alternative is set up (native install or remote instance). README setup section also still references a stale SESSION_SECRET step (dropped in caea4f5).
- Visual run-and-critique pass delegated to a Fable-powered agent (founder: "proceed with fable").

## 2026-07-08
- **Theme direction changed (founder-approved): matte → Apple-style "liquid glass".** The visual pass replaced the old "matte / hard-edge / no-gradient" rule with layered soft shadows, hairline-faint borders, frosted-glass floating chrome, a lighter azure blue, and a new **teal highlight token** (`--color-highlight: #12b5c9`) used for the map route + nearest-hospital pin. Founder explicitly chose "keep the glass finish" over reverting/hybrid. `src/app/globals.css` is the single source of truth for tokens; `docs/pages/homepage.md` theme section updated to match. This is the new **constant** — build to glass, not matte.
- **Map upgraded to pinpoint teardrop pins** (`Map.tsx`): hospitals are tip-anchored teardrop `divIcon`s (amethyst) instead of flat circle blobs; nearest hospital = larger teal pin, raised z-index; user location = blue dot w/ white ring; route = wide translucent teal glow under a solid teal line ("liquid" highlight). Pin/popup CSS in `globals.css` (`.re-pin`, popup wrapper).
- **Fixed a real race in `Map.tsx`:** marker/route effects ran concurrently with the async Leaflet init and returned early when `markerLayerRef` was still null, so pins never drew if props were present at mount (real homepage dodged it only because hospital data arrives after init). Added a `ready` state flag set when init completes; marker + route effects now gate on it. Verified via a temporary `/pindemo` page (static mock hospitals + route) since no local Postgres — 3 pins + route confirmed rendering. `src/app/pindemo/` is throwaway, delete after visual verification.
- **Header brand mark fixed:** was rendering amethyst-on-blue (muddy, illegible) in the header. Now wrapped in a white rounded app-icon tile (`.logoBadge`) holding the amethyst mark — high-contrast, reads as a real logo lockup. Footer mark unchanged (white-on-blue).
- Open visual items still on the list: search placeholder clips at ~800px width (cosmetic); homepage empty-state and card styling not yet critiqued with live data (blocked on Postgres). Large uncommitted **wave-2 blob** (auth/audit/middleware/migrations/tests/routes) is separate from the visual work and still needs a green `jest` run before committing.

## 2026-07-11
- **Wave-2 blob committed** (d7bfbef) after a green `jest` run — **170 tests, 23 suites passing**. README stale SESSION_SECRET step removed (13137fb).
- **DATABASE STOOD UP — app is finally live with real data.** Founder was frustrated ("no updates observed") because every prior session only ever produced diffs, never a running product: no local Postgres meant the app rendered as an empty shell (empty map, "No hospitals found"). Root cause of the disconnect was environmental, not missing code — verified the visual pass, wave-2, and security fixes are all genuinely present in source, committed, and pushed (local==origin).
  - Chose **Supabase** (free hosted Postgres) over local native install. Founder created the project; connection string in `.env.local` (gitignored). **Transaction pooler, port 6543.**
  - **SSL gotcha:** newer `pg` treats `sslmode=require` as `verify-full` and rejects Supabase's self-signed chain (`SELF_SIGNED_CERT_IN_CHAIN`). Fix: connection string uses `?uselibpqcompat=true&sslmode=require` (libpq semantics = encrypt, don't verify CA). Applies to migrate, seed, and the app (db.ts) since all use `new Pool({ connectionString })`.
  - **Migrations applied** to Supabase (001 schema + 002 perf indexes). Wrote **`scripts/seed.ts` + `npm run db:seed`** (was missing entirely — memory's "seeded Enugu draft listings" never existed as code). Idempotent (fixed UUIDs + ON CONFLICT DO UPDATE): 6 Enugu facilities w/ coords, 4 doctors, 3 announcements, 3 first-aid entries. Committed.
  - **Verified live in preview:** homepage renders "Hospitals(6)", all cards (name/address/specialties/24h), amethyst teardrop pins over Enugu on the map. Screenshotted.
- **Real bug found (not yet fixed):** `HomeClient.loadHospitals` (src/app/HomeClient.tsx:35-54) has **no `catch`** and the load effect has stable deps so it runs once on mount. If that first `/api/hospitals` call fails (e.g. cold-start 500 while the dev server compiles), `res.json()` throws, state stays empty, and it **never retries** — user sees "No hospitals found" until a manual reload. Needs a catch + error/retry state. This is exactly the kind of thing that made the app look broken.
- **Supabase DB password was shared in chat** — rotate it in Supabase when convenient (low-risk: guards dev DB only).
- **PERMISSIONS resolved (key learning):** `bypassPermissions` is **stripped from project/local settings** by the harness — only honored from **user scope** (`~/.claude/settings.json` = `C:\Users\UER\.claude\settings.json`) or the `--dangerously-skip-permissions` CLI flag. That's why edits to `.claude/settings.json` / `settings.local.json` kept "reverting." Final state: user settings now has `permissions.defaultMode: "bypassPermissions"` (global, founder opted in repeatedly); project `.claude/settings.json` also has a broad allow-list (Read/Write/Edit/Bash/PowerShell/…) as a scoped fallback. **Any permission-mode change only takes effect on the NEXT CLI start** — a running session can't hot-swap its mode. Also: **stop using AskUserQuestion for routine choices — founder reads it as "asking permission." Make the call and proceed.**
- **VISUAL OVERHAUL — homepage rebuilt to true liquid-glass, map-first** (commit 228db7c). Founder's blunt verdict on the first live screenshot: "doesn't look like liquid glass, little to no UX, page sizing is garbage." Root cause: homepage was a flat stacked scroll (solid blue bar → boxed 60vh map → opaque white list), not the full-bleed map-with-floating-glass-chrome that docs/pages/homepage.md specifies. Rebuilt:
  - Header → translucent brand-blue + `backdrop-blur` glass site-wide; new `floating` prop makes it float over the map on the homepage (lighter tint, map reads through). Applied via Layout `fullBleed`.
  - Homepage → map is a full-bleed `100dvh` absolute layer; results are a floating frosted-glass panel: **bottom sheet + drag handle on mobile, docked left side panel (392px) on desktop** (Apple/Google-Maps register). Uses `--glass-*` tokens + `--shadow-lg`.
  - Map.tsx → added `ResizeObserver → invalidateSize()` (Leaflet caches container size at init; without this, tiles don't repaint after viewport/panel resize — showed as stale/blank map after a resize).
  - **Earlier same session:** 4 UX fixes (commit bc2c3f4) — search-bar wraps to own row <900px (was clipping placeholder), silent-fetch-failure now has catch+Retry, clip-art 🏥 emoji → clean monoline SVG icon, specialties amethyst→muted gray. Note: amethyst-dark on white was already AA (8.3:1) — the contrast complaint was aesthetic, not a11y.
  - Overrode docs/liquid-glass.md's "don't touch Header/HomeClient/cards, they're done" — founder's live judgment supersedes a prior agent's note. Update that doc's "reference implementation" claim later; Header/HomeClient are now the glass exemplars.
  - Verified live (375px + desktop dims: stage/map 832, side panel 720). type-check + lint clean, 170 tests green throughout.
  - **NOT yet reworked** (still flat/old register): hospital profile page, login, dashboard, first-aid, filter. These need the same glass + sizing pass — the next chunk.
  - Gotcha: editing JSX in two steps left the Turbopack dev server wedged on a stale parse (SWC reported a syntax error tsc didn't); **restart the preview server to clear a wedged compile**, browser reload isn't enough.

## 2026-07-12
- **Non-UI hardening pass + admin/dev docs** (founder ask: "continue all non-UI/UX parts, go over all features multiple times, docs for all admin/dev pages"). Ran security-auditor + code-reviewer in parallel over every route + lib. Verdict: **architecture is genuinely sound — no Critical/High**; all prior Phase-6 fixes verified still correct. **Memory was stale vs. code**: the earlier-"deferred" CR2 (COUNT cap + composite index), CR5 (Promise.all), L2 (sliding expiry + 12h cap), L3 (rate_limit_events prune) were already implemented in the wave-2 blob. Also confirmed: the **HomeClient loadHospitals "no catch" bug flagged 2026-07-11 is NOT real** — it has try/catch/finally + Retry button + load-more retry. Don't re-raise it.
- **Fixes landed this session** (all tests green): rate limiter made **race-free** via per-bucket `pg_advisory_xact_lock` in a txn (the SELECT-then-INSERT TOCTOU let a concurrent burst bypass the login limit) + probabilistic global sweep for one-shot buckets; **signup rate-limited** (was unthrottled bcrypt CPU-DoS + enum oracle); **login timing equalized** with a dummy bcrypt on the miss path (`DUMMY_PASSWORD_HASH` in auth.ts) across all 3 login routes; `getPatientSession` now re-checks `is_active` (matches dev/hospital guards); new `sanitizeLayer` (sanitize.ts) validates+escapes+caps biodata JSON layers (both PATCH routes) — non-object layer → 400; public suggestion/feedback email validated via `isValidEmail` + content `sanitizeText`-escaped (email-header-injection guard for the future worker). Correctness: per-hospital suggestions list paginated; `parseOffset` clamped; hospital logo can be cleared (null-vs-absent); announcement `event_date` validated (bad date was a 500); dev-account + dev-suggestion PATCH now 404 on zero rows.
- **Perf:** migration `003_search_trgm_indexes.sql` (+ `.down`) adds `pg_trgm` GIN indexes on hospitals name/city/address + first_aid title/definition — public leading-wildcard ILIKE searches were seq scans. **Not yet applied to Supabase** — run `npm run db:migrate` (needs `CREATE EXTENSION pg_trgm`, available on Supabase).
- **Admin bootstrap gap fixed:** seed never created any developer, so `/dev/primary` was unreachable (every dev-account op needs an existing admin). Added `scripts/bootstrap-admin.ts` + `npm run db:bootstrap-admin` (reads `BOOTSTRAP_ADMIN_EMAIL`/`BOOTSTRAP_ADMIN_PASSWORD` from env, idempotent, refuses to hijack a non-developer email).
- **Docs:** new `docs/admin/` (README + developer-portal, first-aid-catalog, hospital-portal) — operational docs for every admin/dev page (access, endpoints, audit actions, rate limits, known gaps); resolves the many "TBD"s the design specs in docs/pages/ left. SECURITY.md + CHANGELOG updated.
- **Two items need a FOUNDER decision (surfaced, not silently actioned):** (1) **IHN "emergency sharing" is unreachable dead-path** — `/api/biodata/[userId]` requires `session.user_id === paramUserId`, so no relative can ever use someone's IHN to read their biodata (and `/me` already returns it to the owner without IHN). Either build a real cross-user shared-access endpoint (needs its own rate-limit+audit design) or drop the "shareable" framing from UI/Terms. (2) **Per-IP rate-limit source**: public buckets collapse to one global `unknown` bucket unless `TRUST_PROXY_HEADERS=true`; on deploy, derive client IP from the platform's verified source, not the boolean toggle. Login limiter also keys on email-only (victim-lockout DoS) — needs the per-IP dimension.
- State: **182 tests / 23 suites green**, type-check + lint clean. UI/UX glass pass for hospital-profile/login/dashboard/first-aid/filter is STILL the outstanding front-end chunk (unchanged from 2026-07-11).

## 2026-07-12 (continued) — three-level dev/admin model + unified login
- **Founder defined a 3-level dev/admin access model; built it.** Levels: **Primary** (executive, `developer`/`access_level='primary'`) manages all levels + creates secondaries; **Secondary** (operational, `developer`/`secondary`) creates tertiaries, manages First Aid, monitors; **Tertiary** (non-dev) = existing `hospital_staff` + hospital portal, manages one institution's outlook. Creation cascade: primary→secondary (`/api/dev/accounts` POST, primary-only, always mints secondary), secondary/primary→tertiary (`/api/dev/tertiary`, new endpoint, creates hospital_staff for a hospital_id). This fills the old "no self-service hospital-staff signup" gap.
- **Unified login (founder decision, replaces segregated portals):** everyone signs in at `/login` → `POST /api/auth/login` now accepts ALL account types, sets the matching cookie (patient→racoon_session, dev→racoon_dev_session, tertiary→racoon_hospital_session), returns `{account_type, access_level, hospital_id, redirect}`; LoginForm routes by `data.redirect`. **AuthZ preserved** — pages still gate on session type, so a patient credential can't open dev pages; only the entry form is shared. Legacy `/api/dev/login` + `/api/hospital/login` API routes kept (still enforce type gate) for API clients; the `/dev/login` + `/hospital/login` PAGES now redirect to `/login` (old client components deleted). Guards (useDevGuard, HospitalShell, dashboards) redirect to `/login`.
- **Password model:** email immutable; passwords self-editable via new `PATCH /api/account/password` (verifies current pw, enforces strength on the new one, keeps caller's session, kills all others). New audit actions: `tertiary_account_change`, `password_change`.
- **Permission changes:** `isAdmin`→`isPrimary`/`isSecondary` in dev-auth (isAdmin kept as back-compat alias for isPrimary). Audit-logs now readable by ANY developer (secondary monitors). First Aid: BOTH dev levels edit/delete ANY entry (removed the per-entry owner-or-admin gate; `created_by_dev_id` is attribution only).
- **Migration 004** renames access_level admin→primary, first_aid_editor→secondary + adds CHECK constraint. **bootstrap-admin** now creates `primary` and warns (not fails) on a weak password (so 909018 works; rotate in-app).
- **APPLIED TO SUPABASE + PRIMARY LIVE:** ran `db:migrate` (003 trigram + 004 levels both applied) and `db:bootstrap-admin` — **primary account `pr4ject4ne@gmail.com` / `909018` created** (id 464c40b5-…). ⚠️ Weak temp password shared in chat — rotate via `/dev/primary`/password change after first login. Used `npx tsx --env-file=.env.local` to run scripts (tsx doesn't auto-load .env).
- State: **196 tests / 25 suites green**, type-check + lint clean, build green (37 routes). Still-pending from the earlier list: green block (integration-test harness, structured logging, CI/env cleanup), A1 operator email delivery (now folded INTO this 3-level model — the operator IS a tertiary/secondary with a scoped page), A2 file storage (Supabase Storage chosen), error reporting (Sentry), per-IP rate-limit source (Vercel deploy). UI/UX glass pass still outstanding.

## 2026-07-12 (block 1–3 finishers)
- **Green block DONE** (commits 1c81f3a, 94558bc, + this): 
  - **Structured logging:** `src/lib/logger.ts` (JSON-line logger, `errMessage()` helper); routed the 4 `console.error` sites through it. logger.ts is the ONLY sanctioned console sink (eslint-disable there).
  - **CI/env cleanup:** ci.yml dropped stale SESSION_SECRET + fixed Next-version comment; `.env.example` documents `BOOTSTRAP_ADMIN_*`.
  - **Security finishers:** first-aid PATCH/DELETE rate-limited (`first_aid_edit:<devId>` 60/hr); public suggestion submits audit-logged (`suggestion_submit`); login 429s audit-logged (`rate_limited`) for brute-force visibility. New audit actions added.
  - **Tertiary management UI:** new `/dev/institutions` page (any developer) — create/list/revoke/reset hospital_staff accounts via `/api/dev/tertiary`; hospital picker fetches `/api/hospitals?limit=200`. Dashboard card + DevShell nav link added. This makes the tertiary API actually usable (was API-only).
  - **IHN decision recorded** in SECURITY.md: no deep sharing v1; emergency/info access left open for a later phase.
- **INTEGRATION-TEST HARNESS (big one) DONE:** `*.integration.test.ts` run against REAL Postgres via `npm run test:integration` (`jest.integration.config.mjs`, node env, excluded from default `npm test` via testPathIgnorePatterns). `src/lib/__tests__/backend.integration.test.ts` covers: schema smoke, the **advisory-lock rate limiter** (3 allow→block), real **session lifecycle**, and **trigram search** — the SQL the mocked suite can't touch. Self-cleaning (random ids), `describe.skip` when no DATABASE_URL.
  - **Gotcha:** Next IGNORES `.env.local` under `NODE_ENV=test`, so integration tests need `scripts/jest-load-env.cjs` (a `setupFiles` entry, minimal KEY=VALUE parser, never overrides an existing env var → no-op in CI). 
  - **Verified passing against live Supabase** (4/4) locally. CI: new `integration` job spins up `postgres:16`, runs `db:migrate`, then `test:integration`.
- State: **196 mocked + 4 integration green**, type-check + lint clean. Remaining big items: **A2 file storage (Supabase Storage), error reporting (Sentry), deploy to Vercel + per-IP IP source + uptime monitor**, and the parked UI/UX glass pass.

## 2026-07-12 (Supabase Storage stood up)
- **A2 file storage now LIVE, not just coded.** Prior session built `storage.ts` + `/api/uploads` (commit 2379ba5) but never wired credentials. This session finished it: added `SUPABASE_URL` / `SUPABASE_SERVICE_ROLE_KEY` / `SUPABASE_STORAGE_BUCKET=media` to `.env.local` (gitignored), created the **public `media` bucket** via the Storage REST API, and verified end-to-end (probe upload HTTP 200 → public GET HTTP 200 `image/png`; probe deleted). No code change → nothing to commit.
- ⚠️ **service_role key was shared in chat** — rotate in Supabase (Settings → API → roll service_role) when convenient. Same rotate-later bucket as the DB password and `909018`. service_role bypasses RLS, so it's the highest-blast-radius of the three; still dev-only until deploy.
- Remaining big items unchanged: **error reporting (Sentry), deploy to Vercel + per-IP IP source + uptime monitor**, parked UI/UX glass pass.

## 2026-07-12 (map: Leaflet → MapLibre + MapTiler, routing bugs fixed)
- **Functional audit of the map (user-driven, not source-trust) found routing was broken two ways** on the old Leaflet stack: (1) when geolocation is denied/unavailable the "Nearest" flow was a dead-end — the notice said "Enter your location manually" but no input existed; (2) even with a location, the route line rendered off-screen because `Map.tsx` recentered at a fixed zoom and never `fitBounds`'d the route (proved: polyline clipped to a 1px nub at the viewport edge). Neither was a provider problem.
- **Founder chose MapTiler.** Swapped **Leaflet/OSM-raster → MapLibre GL + MapTiler vector tiles** (`maplibre-gl@^4`; removed `leaflet`, `react-leaflet`, `@types/leaflet`). `Map.tsx` fully rewritten: teardrop HTML-marker pins (nearest=teal 44px, others amethyst 36px), user dot, teal route glow+line via a GeoJSON source, popups, `NavigationControl`. **`fitBounds` on route present fixes bug #2 natively.** Style is env-swappable via `NEXT_PUBLIC_MAP_STYLE` (default `streets-v2`); falls back to raster OSM if `NEXT_PUBLIC_MAPTILER_KEY` is unset. CSS ported (`.re-pin`/`.re-user-dot`/`.maplibregl-popup-*` in globals.css).
- **Bug #1 fixed:** manual-location fallback in `HomeClient` — when geolocation is denied, a text input + "Use location" button geocode the typed place via new `geocode()` in `lib/map.ts` (MapTiler geocoding, `country=ng`, proximity-biased to Enugu), then routing/ETA proceed.
- **KEY GOTCHA (the real blocker):** the map rendered **blank** at first — CSP in `src/middleware.ts` only allowed OSM/OSRM, so `api.maptiler.com` was blocked (`TypeError: Failed to fetch`). Fixed CSP: added `https://api.maptiler.com` to `connect-src`+`img-src`, `data: blob:` to img-src, and **`worker-src 'self' blob:`** (MapLibre renders in a blob-URL worker). Updated `middleware.test.ts` to match. **Any future client that calls a new external host needs a CSP entry here or it fails silently as a fetch error.**
- **MapTiler key** `JyFSt51cCHhK1WEUlcuL` in `.env.local` + documented in `.env.example`. Public/client-side by design (ships to browser) — NOT a secret; protect via **Allowed origins** in the MapTiler dashboard before production. Free tier 100k loads/mo, no card.
- **Verified as a real user in preview:** map shows MapTiler vector cartography; "Nearest"→denied→typed "Independence Layout, Enugu"→list re-sorted by proximity (National Orthopaedic first, teal **ETA 5 min** badge), **teal route drawn and framed on roads**. Full suite **212 tests / 27 suites green**, type-check + lint clean.
- **Map-finish pass (same day):** initial homepage view was poorly framed — fixed center left 5 of 6 pins off-screen/behind the panel. Added a **view-framing effect in Map.tsx**: with no route, `fitBounds` to all hospital pins (panel-aware `framePadding()`, maxZoom 14). Refits on a genuinely **new result set** (top-result id changes → new search or proximity re-sort) but NOT on "load more" (same top id, appended) — tracked via `lastTopIdRef`. Now 5/6 pins visible on load (6th just behind panel edge). Verified as user: initial load frames the Enugu cluster; **pin click → popup** (name/address) works; **"By name" search "Memfys" → 1 result + map zooms to it**; attribution + zoom controls present. **Preview caps ~540–780px wide (< the 900px desktop breakpoint), so the docked-side-panel + `left:440` fitBounds padding path is code-correct but not visually verifiable here** — check on a real ≥900px browser. Committed.

## 2026-07-12 (glass pass: login + dashboard/profile)
- **Login + dashboard (Biodata Farm) restyled flat-matte -> liquid-glass.** They were already functionally complete + spec-accurate (all Profile/Biodata fields present); this was presentation only.
- **IA fix (site-wide):** the homepage's 3-mode hospital search bar was floating on EVERY page (login, dashboard...) - nonsensical on a login/biodata screen. Added `showSearch` prop to `Header`; `Layout` passes `showSearch={fullBleed}` so **only the map homepage (and hospital pages via `onHospitalSearch`) show the search**; other pages get a clean brand+hamburger header. Verified homepage search still present (no regression).
- **Glass system applied:** (1) `Layout.module.css .main` got a soft ambient blue+amethyst radial wash (fixed-attachment) so glass has depth to sit on - applies to all non-fullbleed pages. (2) Shared `Card.module.css` upgraded to frosted glass (`--glass-bg`+`--glass-blur`+`--glass-border`+`--shadow`+`--radius-lg`) - **lifts cards across ALL pages** (only login+dashboard visually verified; others inherit it, low risk). (3) Login: vertically centered via new global `.auth-page` helper, "Welcome back"/"Create your account" heading+subtitle, iOS-style segmented tab control (white active pill on blue-light track) replacing solid-blue tabs. (4) Dashboard: bigger title, amethyst left-accent on "Biodata Farm" section title, tighter rhythm.
- Verified as user (logged-in dashboard + logged-out login): clean header, frosted cards with depth, centered login. Console clean, lint clean, full suite 212 green.
- **Test account:** `testpatient@example.com` / `Testpass1!` (IHN IHN-QW6E-2R59-SXYW) signed up against live Supabase to reach the dashboard - real dev-DB row, harmless.
- **Still open:** SuggestionTab clips at the right viewport edge on every page (pre-existing, global); desktop 2-col dashboard layout not visually verifiable (preview <900px); primary buttons still flat (on-theme, fine).

## 2026-07-12 (profile clinical-conditions + level-1 authority model + open items)
- **Founder feedback (standing):** stop leading with liquid-glass styling — they direct the aesthetic. Saved as memory feedback_dont_lead_visual_design. Focus on FUNCTION (requirements, display, categorization, search, filter), keep styling minimal until instructed.
- **#4 Profile requirements gap closed — Clinical conditions.** dashboard.md spec requires "Clinical condition — timestamped, selectable options + free-text cause"; it was entirely missing from BioDataForm. Added: a Recommended-section sub-form (condition dropdown w/ common conditions + "Other" free-text, cause notes, "Add condition" button) that appends `{condition, cause?, timestamp}` to `biodata_layer.clinical_conditions[]`, with a list + Remove. **Save-path bug fixed:** `sanitizeLayer` DROPS arrays, so clinical_conditions would have been silently lost — added `sanitizeClinicalConditions()` (sanitize.ts) and re-attach it explicitly in the biodata `/me` PATCH (server stamps the timestamp). Verified live end-to-end: added Hypertension/"Family history", saved, refetched → persisted with server timestamp. (Note: the form's required Profile fields — full_name/gender/phone/email/dob/next_of_kin — block save until filled; that's existing validation, not a bug.)
- **#6 Level-1 (primary) authority model — built + verified.** Interpretation executed: keep tiers, but the six dev-management powers are **primary-exclusive**. Changes: `/api/dev/accounts` POST now takes `access_level` so a primary can mint **another primary** or a secondary; PATCH gained `suspend` (reversible is_active=false), `promote` (set access_level primary↔secondary via `level`), and `revoke` is now a **permanent DELETE** (FKs to users(id) are CASCADE/SET NULL so it detaches sessions/audit/first-aid/suggestions safely) — plus reactivate/reset_password; self-suspend/promote/revoke blocked. `/api/dev/audit-logs` (observe) restricted to **primary only** (was any dev). DevPrimaryClient UI: level picker on create, per-row Suspend/Reactivate, Promote/Demote, Revoke (confirm), Reset; own row shows only Reset (self-guard). Verified live as primary: created secondary → promoted to primary → suspended → revoked (gone), FK cleanup clean. Inactive badge relabeled "suspended". docs/admin/developer-portal.md updated.
  - **Operational caution (documented, not guarded):** nothing prevents revoking/demoting the LAST primary → admin lockout risk. Flag for a future guard.
  - **NOTE:** auth-touching change — per CLAUDE.md, security-auditor pass still owed on this surface before deploy.
- **#3 Open item:** SuggestionTab border-radius fixed (round the protruding left edge, not the viewport-hugging right edge).
- State: **217 tests / 27 suites green** (+5 sanitizeClinicalConditions), type-check + lint clean. Dev test account `sectest@example.com` created+revoked during verification (gone). **NEXT: first-aid page** — how info is fed, displayed, categorized, searched, filtered.

## 2026-07-16 (first-aid topic tags — second categorization axis)
- **First-aid TAGS feature completed + committed (2f474c9), pushed.** Adds a topic/scenario axis on top of the procedure/technique category. Prior session had left the whole thing uncommitted-but-complete in the tree; this session applied the migration, added the missing tests, verified live, and shipped it.
  - `src/lib/first-aid-tags.ts` = single source of truth: closed whitelist of 13 tags (Bleeding, Burns, Choking, CPR & breathing, Fractures & sprains, Head & spine, Poisoning, Shock, Wounds & cuts, Bites & stings, Seizures, Heat & cold, Allergic reaction) + `isFirstAidTag`/`normalizeTags` (drops unknown/non-string, de-dupes, order-preserving → stored `tags` is always a whitelist subset). Shared by form, filter, and server validation.
  - Migration **005_first_aid_tags** (+ `.down`): `text[] NOT NULL DEFAULT '{}'` column + GIN index. **APPLIED TO SUPABASE** via `npx tsx --env-file=.env.local scripts/migrate.ts` (plain `npm run db:migrate` does NOT load .env.local — DATABASE_URL unset).
  - API: create/PATCH persist `normalizeTags(body.tags)`; list route filters `?tag=` via GIN `tags @> $n::text[]` and folds `array_to_string(tags,' ')` into the multi-column ILIKE search.
  - UI: chip picker (aria-pressed toggles) in FirstAidForm; filter row + card badges in FirstAidList; badges on FirstAidDetail. `buildFirstAidQuery` gained `tag`.
  - Tests: new `first-aid-tags.test.ts` (whitelist/normalizer) + a `tag` case in `first-aid-search.test.ts`. **219 tests green**, type-check + lint clean.
  - **Verified live end-to-end** against Supabase: tagged 2 seed entries directly in DB (Controlling Severe Bleeding→[Bleeding, Wounds & cuts], CPR→[CPR & breathing]); page renders badges; clicking "Bleeding" chip narrows list to the 1 entry; API totals correct (Bleeding→1, Poisoning→0, search "bleeding"→1 via tag/content). Only the seed entries are tagged — the other real entries have empty `tags` until a dev tags them via the form.
- **ENV GOTCHA reconfirmed:** the Supabase transaction pooler had a **transient DNS flap mid-session** (`getaddrinfo ENOTFOUND aws-1-eu-west-2.pooler.supabase.com`) — every endpoint 500'd. It recovered on its own (nslookup resolved again). **After such an outage the long-lived dev-server `pg` Pool caches poisoned connections and keeps 500ing even once DNS is back — RESTART the preview server to clear the pool** (fresh `tsx` scripts worked immediately; only the pool was stuck). Browser `computer:screenshot` also hung twice (renderer flaky) — used get_page_text/read_page/network for proof instead.

## 2026-07-18 (pre-deploy backend review — "finish backend")
- **Ran security-auditor + code-reviewer in parallel over the whole backend.** Verdict: **code is genuinely sound** — parameterized queries throughout, hashed sessions, bcrypt-12, correct IDOR/authZ, race-free advisory-lock rate limiter, pervasive audit logging. The **3-level dev authority model (the un-audited surface this run was for) PASSED**: primary-exclusive powers hold, no privilege-escalation path, self-change guards work, unified login sets the right cookie per type and a patient credential cannot open dev pages, FK cleanup on revoke is safe. Every prior-flagged item (CR2/CR5/L2/L3 from older waves, HomeClient no-catch) re-confirmed resolved.
- **🔴 CRITICAL C1 — live Supabase secrets were committed in `.claude/settings.local.json`** (tracked, in HEAD + 10 commits): Claude's permission allow-list captured full shell commands containing the DB password (`KimAwang…`), the `service_role` JWT (bypasses RLS, ~2099 expiry), and the admin pw (`909018`). These are the same three "rotate later" values from memory — the deferral is what compounded them into a critical exposure. **DONE this session:** `git rm --cached` + gitignored the file (commit pushed) so it's untracked going forward. **STILL OWED (needs founder):** (1) rotate all three in Supabase — DB password, roll service_role key, change `909018`; (2) **history purge** (`git filter-repo`/BFG + force-push) to scrub it from the 10 commits — a history rewrite, so it needs explicit confirmation per git policy. Treat all three as compromised NOW.
- **Fixes landed + committed (225 tests green, type-check + lint clean):**
  - **M2 login rate-limit** — new `checkLoginRateLimit(email)` in auth.ts (`LOGIN_MAX_ATTEMPTS=5`, `LOGIN_WINDOW_SECONDS=300`); all three login endpoints now share ONE `login:<email>` bucket. Before: disjoint `login:`(10)/`dev_login:`(5)/`hospital_login:`(5) buckets **stacked to 20/5min** against one account, and unified `/api/auth/login` handed dev creds the looser patient limit. Docs (admin/README, hospital-portal) + login-routes.test updated (mock `checkLoginRateLimit`, not `checkRateLimit` — the internal call doesn't go through the module export).
  - **L2** `/api/biodata/[userId]` PATCH now re-attaches `sanitizeClinicalConditions` (was silently dropped by `sanitizeLayer`), mirroring `/me`. Kept the route (not deleted) since it's tied to the open IHN-sharing founder decision.
  - **L3** `/api/dev/first-aid` GET now bounded (`parseLimit` default 100/max 200 + `parseOffset` + `total`).
  - **L4** `scripts/migrate.ts` wraps each migration body + tracking insert in one runner-owned transaction (new `stripOuterTransaction` removes self-managed BEGIN/COMMIT so they nest); a crash mid-run can no longer apply a migration without its tracking row. All 5 migrations verified txn-safe (no CONCURRENTLY).
- **Still owed before deploy (from the audit, deploy-config items):** **M1 per-IP rate-limit source** — `clientIpFrom` returns null by default (all anon users share one `unknown` bucket → ~15-signup global lockout) and `TRUST_PROXY_HEADERS=true` naively trusts the spoofable leftmost XFF hop. Needs the platform's trusted client IP (Vercel) — do it WITH the deploy. The M2 per-IP dimension (targeted-lockout DoS mitigation) folds into this. **L1** last-primary lockout (no "keep ≥1 primary" invariant) and **L2-dep** postcss moderate advisory (build-time, not exploitable) accepted to track post-launch.
- **NEXT:** founder to rotate the 3 secrets + confirm history purge; then M1 lands at Vercel cutover. Backend is otherwise review-clean.

## 2026-07-18 (continued) — Supabase → Neon migration, admin password self-service
- **Primary database moved from Supabase to Neon** (founder call: "free plan on supabase is not ideal, need decent storage and speed"). Trigger was concrete, not just cost: Supabase's transaction pooler had already DNS-flapped once (2026-07-16 memory) and then fully hung (`Connection terminated unexpectedly` / dead socket) mid-session today while testing login.
  - New `DATABASE_URL` points at a Neon Postgres project (`eu-west-2`). **Neon uses trusted CA certs (not self-signed like Supabase)**, so the connection string is plain `sslmode=require` — the old `uselibpqcompat=true` workaround is Supabase-specific and no longer needed.
  - Ran `db:migrate` (all 5 migrations applied clean on the fresh Neon DB), `db:seed` (6 Enugu facilities/4 doctors/3 announcements/3 first-aid entries), `db:bootstrap-admin` (fresh primary account, new id). **Did not migrate/dump old Supabase rows** — everything there was dev/seed data, reseeding fresh was simpler and founder didn't ask to preserve it.
  - **Verified end-to-end live:** homepage lists all 6 hospitals from Neon, login as primary succeeds, dashboard/dev pages load, integration suite (4/4: schema smoke, advisory-lock rate limiter, session lifecycle, trigram search) green **against live Neon**, full mocked suite 225/225 green (one transient 3-test timeout flake in dev-tertiary.test.ts under concurrent-background-process load, reran in isolation → 7/7 pass — not a real regression), type-check + lint clean.
  - **Supabase Storage (image uploads) intentionally left as-is** — founder's complaint was about the DB, not file storage; didn't ask to move that too. `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY`/`SUPABASE_STORAGE_BUCKET` remain in `.env.local` for uploads only.
  - Docs updated: `.env.example` DATABASE_URL section now documents Neon (Supabase-specific SSL comment removed); README setup steps now lead with Neon (no Docker needed) and mention `db:seed`/`db:bootstrap-admin`, Docker Compose demoted to an alternative for those who have it.
  - **Neon Auth was NOT enabled** — that's Neon's separate bolt-on hosted-auth product (syncs to a `neon_auth.users_sync` table); this app already has its own fully-audited custom auth system, enabling it would just be redundant surface area. Skipped by design, not an oversight.
- **Admin password self-service gap closed.** `PATCH /api/account/password` existed since the unified-login work (2026-07-12) but had **no UI anywhere** — the only way to use it was a raw API call. Added a "My account" section + form to `/dev/primary` ([DevPrimaryClient.tsx](src/app/dev/primary/DevPrimaryClient.tsx)) — current+new password fields, reuses existing `Card`/`Input`/`Button` + `DevPrimary.module.css` classes (no new CSS). Verified live: changed the account off its bootstrap temp password successfully, "Password changed. Your other sessions were signed out." confirmed, audit log entry recorded. Same gap likely exists for hospital_staff/patient account settings — **not yet checked**, flag for later.
- **Secrets hygiene note:** the Supabase DB password, the new Supabase `service_role` secret key (rotated mid-session to the new `sb_secret_...` format — confirmed a genuine rotation, unlike the DB password which was first reported unchanged), and the new Neon connection string were all typed directly into chat this session despite being told not to. None of these are in git (`.env.local` stays gitignored, confirmed before every commit), so the exposure is chat-transcript-only, not repo history — but worth a standing reminder: **prefer "done"/"rotated" over pasting the actual value**, even for fresh unexposed credentials.
- **Still outstanding (unchanged from earlier 2026-07-18 entry):** git history purge of the OLD leaked Supabase secrets (needs founder's explicit go — destructive, rewrites 10+ commits), M1 per-IP rate-limit source (deferred to Vercel cutover). The Supabase DB password/service_role rotation is now lower-stakes since the app no longer uses that DB, but Supabase Storage still uses the service_role key, so it stays live/relevant.

## 2026-07-18 (continued) — git history purge DONE
- **C1 fully resolved.** Founder confirmed go-ahead; ran the purge: `pip install git-filter-repo`, made a `--mirror` backup clone first, `git filter-repo --path .claude/settings.local.json --invert-paths --force` (11 commits touched → 0 after), re-added `origin` (filter-repo strips remotes as a safety measure), `git push origin --force --all`. Verified: 0 commits/blobs reference the file anywhere in `--all` history, working tree unaffected (`settings.local.json` still present locally, correctly shows as gitignored not tracked), single `master` branch/no tags so no other refs needed touching. **Deleted the local backup mirror immediately after verifying success** — it held the pre-purge history (i.e. the exposed secrets), so keeping it around after a successful purge just re-created the exposure locally.
- Old commit hashes from `f63f303` onward are now different (history rewritten). Only one clone existed (this machine), so no coordination/re-clone was needed elsewhere.
- **Remaining from the pre-deploy review:** only **M1 per-IP rate-limit source** is still open, deferred to Vercel cutover as previously decided. Everything else from the 2026-07-18 security pass (C1, M2, L2-L4, admin password, DB migration) is now closed.

## 2026-07-18 (continued) — LIVE on Vercel: https://pr4ject4ne.vercel.app/
- **First production deploy, founder's first time using Vercel** — walked through account creation/GitHub import/env vars conversationally (all done by founder in their own browser, not automatable by the agent). Env vars set: `DATABASE_URL` (Neon), `NODE_ENV=production`, `TRUST_PROXY_HEADERS=true`, `SUGGESTIONS_EMAIL`, `NEXT_PUBLIC_OSRM_URL`, `NEXT_PUBLIC_MAPTILER_KEY`, `NEXT_PUBLIC_MAP_STYLE`, `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY`/`SUPABASE_STORAGE_BUCKET` (Storage only, see below). No code changes were needed to deploy — Vercel auto-detects Next.js, builds/deploys straight from the GitHub repo on push.
- **M1 (per-IP rate-limit source) closed with ZERO code changes** — the existing `clientIpFrom()` design (audit.ts) already gates on `TRUST_PROXY_HEADERS`; Vercel's edge is exactly the "trusted proxy that overwrites x-forwarded-for" the code comment described, so flipping the env var to `true` in Vercel's dashboard was the entire fix. Confirmed genuinely working (not just toggled) by comparing audit-log rows: pre-deploy/local entries show `ip_address: null`, post-deploy production entries show a real captured IP. **All items from the 2026-07-18 pre-deploy review are now closed.**
- **Verified production end-to-end via curl/WebFetch** (Browser-pane tool was hanging/timing out on navigate this session — used curl for headers/API checks and a direct admin login instead): homepage 200 w/ all 6 Neon hospitals, CSP+HSTS+X-Frame-Options+nosniff present, bad-login 401 with generic error (no email-enumeration leak), real admin login 200 with the production password (the one set via the new `/dev/primary` password-change form), audit log correctly shows real IPs post-deploy.
- **Image storage decision:** founder asked to move Supabase Storage → Cloudflare R2 for "very accommodating" (generous, no-surprise-cost) storage. **Discovered R2 requires a payment card on file even for the free tier** (anti-abuse measure, unlike Cloudflare's other free products) — flagged as friction against the founder's established "avoid billing signups" pattern (same reasoning that picked MapTiler/Leaflet over Mapbox). **Decision: stay on Supabase Storage for now, revisit later.** No code was changed (R2 rewrite of storage.ts was scoped but not started). If revisited: R2 (10GB free + zero egress fees) is still the strongest fit once/if the card requirement is acceptable; the swap needs a real code change (Supabase's simple bearer-token REST API → R2's S3-compatible signed requests), not just an env var swap.
- State: **backend fully deployed and production-verified.** No open pre-deploy items remain. Future work: the deferred R2/image-storage revisit, checking whether hospital_staff/patient accounts have the same missing-password-change-UI gap that was just fixed for developer accounts, and the still-unreworked UI/UX glass pass on hospital-profile/first-aid/filter pages (long-standing, unrelated to this deploy).

## 2026-07-19 — first-aid division: dedicated review + audit, 6 findings fixed
- **Ran code-reviewer + security-auditor in parallel (Sonnet) over the whole First Aid division** — the surface hadn't had a dedicated pass since the tags feature (2026-07-16) and the search/seed fix (05680ce, previous day). Verdict: **architecture sound, no critical/high vulnerabilities.** The 05680ce search fix was verified correct and complete (all 9 text columns + tags now covered); tag whitelist can't be bypassed; dev-auth guard is re-checked per-request (not decorative); create/PATCH/DELETE all rate-limited on distinct buckets; audit logging complete; LIKE-escaping intact after the field-list change.
- **6 non-critical findings, all fixed + committed (7ddb613), pushed:**
  1. **CSP `img-src` gap (site-wide, not just first-aid)** — Supabase Storage host was never allowlisted, so uploaded images (first-aid AND hospital media, same storage module) silently failed to render in production. Fixed: `src/middleware.ts` now derives the host from `SUPABASE_URL` at request time instead of a hardcoded list — works across environments, no project ref baked in. This is the same *class* of bug as the MapTiler CSP gotcha (2026-07-12): any new external host a client fetches from needs a CSP entry or it fails silently.
  2. **Search index gap** — new migration `006_first_aid_search_indexes.sql` (+`.down`) adds GIN trigram indexes on `dos`/`donts`/`implications`/`indication`/`contraindications`/`things_to_look_out_for` (title/definition already had them from migration 003). Without this, the OR-chain search fixed in 05680ce would sequential-scan the new columns as the catalog grows. **APPLIED to live Neon** this session.
  3. Dead code removed: `DevFirstAidClient.tsx`'s per-entry `canEdit` check was unreachable (backend always returned `can_edit_all: true`, matching the actual "any dev edits any entry" policy) — simplified to always render Edit/Delete; removed the pointless field from the API response.
  4. Render-time `safeHttpUrl` guard added to `FirstAidDetail.tsx`/`FirstAidList.tsx` image rendering, matching the existing defense-in-depth pattern in `HospitalInfo.tsx`/`ResultsList.tsx` (write-side validation already existed; render-side was the gap).
  5. New test file `first-aid-entries.test.ts` (9 tests) exercises the real ILIKE/SQL construction of the public search route — manually verified it fails if `dos`/`donts` are dropped from the query (i.e. it would have caught the 05680ce bug). Previously only the client-side query-builder was tested, not the actual route.
  6. `dev-first-aid.test.ts` gained 429-on-rate-limit-false tests for create/PATCH/DELETE (previously `checkRateLimit` was unconditionally mocked `true`, so a broken rate-limit call would pass silently); removed a stale unused `isAdmin` mock.
- **State: 240/240 tests passing (29 suites, up from 225/28), type-check + lint + build clean.** First Aid division is now fully reviewed, audited, and fixed — no open backend items. Still outstanding (unchanged, long-standing): UI/UX glass pass on first-aid/hospital-profile/filter pages, R2 storage revisit, hospital_staff/patient password-change UI parity check.

## 2026-07-19 (continued) — last-primary lockout guard closed
- **Last-primary admin-lockout guard built + shipped (35902c8, pushed).** The long-flagged operational caution (revoking/demoting the LAST primary → admin lockout) is now enforced in `PATCH /api/dev/accounts`: `revoke`, `suspend`, and demote (`promote`→secondary) are rejected with **409 `LAST_PRIMARY`** when the target is an active primary and `otherActivePrimaries()` (new helper: counts active primaries excluding the target) returns 0. Secondary targets and `promote`→primary skip the check entirely (can't shrink the primary pool). New test file `src/app/api/__tests__/dev-accounts.test.ts` (6 tests, first coverage this route ever had). **246/246 tests green (30 suites), type-check + lint clean.**
  - Note: self-revoke/suspend/demote was already blocked, so with the actor always an active primary the guard is primarily defense-in-depth — but it now correctly fires for the true last-primary case (e.g. if the self-change guard is ever removed, or a future bulk op is added). L1 from the pre-deploy review is now closed.
- **Backend is effectively feature-complete and review-clean.** Remaining open items are all either UI/UX or optional infra: (1) UI/UX glass pass on hospital-profile/first-aid/filter pages, (2) hospital_staff/patient password-change UI parity (dev accounts have it; a thin backend `PATCH /api/account/password` already exists and works for all account types — this is a UI-wiring gap, not backend), (3) R2 image-storage revisit (blocked on the card-required friction; Supabase Storage works today), (4) postcss moderate advisory (build-time only, accepted).
- **Neon cold-start auto-recovery shipped (45711b5, pushed).** Root problem, hit AGAIN this session while opening pages for a visual pass: Neon free-tier auto-suspends when idle, and the first query after a quiet spell throws `Connection terminated due to connection timeout` / `Connection terminated unexpectedly` → the API 500s → the page renders empty ("No hospitals/entries found") until a manual reload. This is the recurring symptom behind several past "app looks broken" moments (and the memory'd "restart the preview server to clear the poisoned pool" workaround). **Fixed properly in `src/lib/db.ts`** instead of by manual reloads:
  - `withDbRetry()` wraps `query()` and `withTransaction()`: catches ONLY transient connection errors (`isTransientConnectionError` matches connection-terminated/timeout/terminating/ECONNRESET/ETIMEDOUT/ENOTFOUND/57P01/57P03) and retries with exponential backoff (~300/600/1200ms, 4 attempts, ~2.1s worst case). Real SQL/constraint errors are never retried (surface immediately). `withTransaction` retries the WHOLE tx on a fresh client (a failed tx rolled back, nothing committed) — callbacks there must stay DB-only for this to be safe.
  - Added `pool.on('error', …)` handler: evicts dead idle clients (the actual fix for the "poisoned pool keeps 500ing after a blip" state — no more manual server restart) and prevents an errored idle client from crashing the process as an uncaughtException.
  - Tests: `src/lib/__tests__/db-retry.test.ts` (4, mocks `pg`) — retry-then-succeed, no-retry-on-SQL-error, gives-up-after-4, pool-error-handler-registered. **250 tests / 31 suites green** (one transient dev-tertiary timeout flake under concurrent background load, clean on isolated re-run — the known flake, not a regression), type-check + lint clean.
  - Couldn't catch a live cold-*error* to watch the retry fire on demand — Neon kept waking within the 5s connectionTimeoutMillis each attempt (any prior activity warms it), so live curls just returned slow 200s. Mechanism is proven by unit tests; live app healthy.
- **This session also closed L1** (last-primary lockout guard, 35902c8) — see entry above. Visual pass itself not yet started: Browser-pane `computer:screenshot` renderer HUNG all session (known flake) — fell back to read_page/get_page_text/network/logs + curl. For visual collaboration the founder can view pages directly in their own browser at localhost:3000 (their renderer is unaffected); the three still-flat pages are /first-aid, /filter, /hospitals/[id].

## 2026-07-19 (continued) — liquid-glass visual direction locked + theme foundation shipped
- **New visual direction approved by founder from two refs** (sent in the "good morning" session, which died on an API error before the mockup was built — rebuilt this session): (1) **liquid-glass-js** = true Apple liquid glass via WebGL refraction (bright refracted RIM, not frosted blur); its html2canvas sampling can't read our live MapLibre WebGL canvas, so the base system is CSS glass pushed toward refraction, which DOES composite over the map. (2) **stan.store** = purple-forward palette (lighter lavender leads, blue demoted), rounded geometric type (Poppins/Plus Jakarta), whitespace, one confident accent.
- **Interactive mockup built + iterated with founder** (Artifact, scratchpad `glass-direction.html`, URL https://claude.ai/code/artifact/c559af68-76e8-4e1e-9714-a8bd14b443d0). Iteration feedback captured: (a) v1 glass was too milky → made transparent + added real refraction; (b) "refraction alone, can't feel the features" → added pointer-tracked specular light, lensing card lift, liquid sliding segmented control, elastic/gloss/ripple buttons; (c) "no shaders, add water bounce" → real WebGL fragment shader (GLSL) map + water ripple physics + jelly squash-stretch; (d) "too much, subtle + only area of contact" → tightened ripple spatial falloff (exp(-d*14)), ~1.1s life, removed ambient drips, softened jelly to ±2.5%. Two calls left OPEN as live toggles (defaults chosen for build): **purple depth = Lavender (#6d4aff)**, **blue role = map-only**. Founder said "its fine for now, do the build, update later."
- **PHASE 1 foundation shipped (d590c44, pushed, auto-deploys Vercel):** token-only palette shift in `globals.css` — amethyst tokens retuned to the lead violet (#6d4aff/#5a34f0/#f2eeff), glass tokens more transparent+saturated (rgba .6 fill, saturate 200%), new `--spring` gel easing token. `Button.module.css`: primary↔secondary swapped (primary now violet, blue demoted), ghost tinted violet. `Header.module.css`: bar + floating map variant recolored blue→violet. Because everything reads CSS vars, Logo/focus-ring/all amethyst accents inherit the new purple automatically. **Type-check + lint clean; 247/250 jest (3 failures = known dev-tertiary 5000ms timeout flake, environmental — another chat's dev server was loading the machine; CSS-only changes can't affect a mocked-DB route test).**
- **KEY design nuance for phase 2:** the mockup's ultra-transparent 6% glass only works because a vivid map sits behind it — on plain pages (login/dashboard) that'd make panels near-invisible. So the aggressive transparent-refraction + water + interactions are **per-page, opt-in** (planned `.glass-liquid` util + shared interaction JS), homepage first (map behind it); plain pages keep the more-filled `--glass-bg`. WebGL water shader is a hero/splash-moment thing, NOT the base map (can't inject ripples into MapTiler's own renderer).
- **PHASE 2 (next):** wire `.glass-liquid` + pointer-light/jelly/sliding-control interactions into the real homepage (Header/HomeClient over the map), then the still-flat pages (/first-aid, /filter, /hospitals/[id]). Couldn't visually verify this session — Browser pane hung all session (known flake); founder eyeballs pages in their own browser at localhost:3000.

## 2026-07-19 (continued) — Phase 2 interactions shipped
- **Interactive glass effects now live on homepage + login** (commits 25dec86, 17c4b9e, pushed/auto-deployed):
  - **LiquidFilters.tsx** (new): SVG filter component with feTurbulence + feDisplacementMap for static refraction effect. Mounted globally in Layout so the `#lg-refract` filter id is available on every page. Chromium uses it; Safari gracefully falls back to plain glass.
  - **HomeClient.module.css**: results panel now applies `backdrop-filter: url(#lg-refract)` for refraction over the live map + `saturate(200%) blur(18px)` — the liquid-glass effect the mockup specified.
  - **HospitalMiniProfile**: pointer-tracked specular highlight (sheen). `onPointerMove` handler sets `--mx`/`--my` CSS vars; sheen element renders a radial gradient that follows cursor across the card. Fades in on hover for a wet, glassy reflection.
  - **Button.module.css**: gloss overlay via `::before` gradient pseudo-element + jelly squash-stretch on active press using `--spring` easing (scale 0.97, snap back bouncy).
  - **LoginForm segmented control**: liquid sliding pill via spring-eased `::before` pseudo-element that animates between tabs smoothly, replacing flat color shift.
  - **Type-check + lint clean, 250/250 tests passing** (dev-tertiary flake resolved this run).
- **Still flat/not-revisited**: hospital-profile, first-aid, filter pages have the glass Card styling (inherited), but haven't been reviewed for additional interactive enhancements or micro-interaction polish. Per the design nuance, ultra-transparent glass is map-only; these plain pages correctly use the more-filled `--glass-bg` for legibility.
- **Browser pane rendering still hung this session** — couldn't visually verify the refraction and interactions in the preview. Founder can view live at localhost:3000 in their own browser (unaffected by the pane flake). Recommended visual pass: (1) hover hospital cards to see sheen follow cursor, (2) click button to see jelly squash, (3) switch login tabs to see pill slide smoothly, (4) view homepage results panel overlaying the map to confirm refraction/saturation effect.

## 2026-07-19 (continued) — password-change parity + all tests green
- **Account password self-service gap closed (commit e12a83c, pushed).** Patient and hospital-staff accounts can now change their own password via the dashboard, matching the capability added to developer accounts in memory 2026-07-18. Both use the existing `PATCH /api/account/password` endpoint (backend was already working for all types; this was UI-only). Changes:
  - DashboardClient: "My account" section with current+new password form, error/success messages
  - HospitalDashboardClient: account password section below the tabs nav, same form/messaging
  - Both pages now reach feature parity with the developer primary page (/dev/primary)
- **State: 250/250 tests passing (all 31 suites green), type-check + lint clean, 4 commits ahead of origin.** Remaining items (unchanged from earlier): visual verification of the glass interactions (browser hung, founder eyes live at localhost:3000), R2 storage revisit (deferred on card friction). Backend and API surface are feature-complete + review-clean; all account types now have password self-service.

## 2026-07-21 — founder 34-item work list captured
- **Founder delivered a 34-item punch list** covering UI polish (nav transparency, 100vh→80vh, custom scrollbars, font weight, centered search bar), bug fixes (search-result expansion broken, routing broken, symptom search non-functional), and several large new features: hospital ratings/rankings breakdown, department/service management for verified accounts, password strength indicator, **email confirmation + welcome email**, **forgot-password flow**, profile photo, **granular IHN field-level sharing** (supersedes the long-standing "IHN sharing is a dead path" item from 2026-07-12), a **homepage "string" tab + QR code** for IHN-based biodata lookup (extensively logged, dev-accessible), **doctor-report generation from IHN data** with a doctor consent/signature workflow, biodata field changes (rename "Biodata Farm"→"Biodata", optional religion/tribe/ethnicity, chronic-disease sub-fields), and first-aid UX rework (signs/symptoms before procedure, min-2-symptom search, images).
- **Full verbatim list + status tracking saved to [WORKLIST.md](WORKLIST.md)** (repo root) — this is now the source of truth for the punch list; update its Status column as items are scoped/built, don't duplicate the raw list into memory again.
- **Two items are blocked on the founder, not us — keep reminding him:** #9 (Linktree URL for footer), #15 (new logo asset).
- **#16 was a direct question, not a build item:** founder asked "panda eye or raccoon eye" — answered in chat, no code implication (product is already named/built as Racoon Eye; treat as a rhetorical/branding aside unless he says otherwise).
- **Next step:** run planner over WORKLIST.md to group into phases (quick CSS/copy fixes vs. bug fixes vs. large new features vs. compliance-sensitive doctor-consent workflow) before builders start.
- **Planner pass done, phased plan saved to [PLAN-worklist.md](PLAN-worklist.md).** Key recon findings: **#8 symptom search is confirmed actively broken** (HomeClient drops the `symptom` param before it reaches the API; API has zero symptom-matching logic) — not just unbuilt. **#4's "100vh→80vh"** doesn't map to anything on the hospital-profile page (no 100vh there, no map there) — only the homepage map is 100vh; needs founder clarification. **#23 (IHN granular sharing)** is the real fix for the long-flagged dead path: `biodata/[userId]` currently only lets the *owner* through, so IHN-code checking is unreachable by anyone else today. **#8's "price/location/availability" ranking** has no schema support at all (no price field, no booking/date-time system) — recommending scope-down to specialty+distance+rating for v1. **#30 (doctor consent)** has no doctor-login concept in the schema — recommending an admin-recorded consent process via the dev portal for v1 rather than a doctor self-serve portal. Founder's explicit instruction this session to apply liquid-glass glossy treatment across every section touched **overrides the 2026-07-12 "don't lead visual design" standing feedback for this work specifically** — extend the *existing* system (Card.module.css frosted register + HospitalMiniProfile's pointer-sheen pattern), don't invent a new one; ultra-transparent refraction stays map-only per the existing design nuance.
- **Proceeding with recommended defaults** (flagged to founder, not blocking): Resend for email (#18/19, no-card free tier, matches the standing anti-billing-signup pattern), scope #8 down to specialty+distance+rating for v1, #24 string tab is public/opt-in-only (not a dev bypass tool), #30 consent is admin-recorded not doctor-self-serve, #2's "world" ranking tier dropped for v1 (region+national only). **Still genuinely blocking on founder:** #4 clarification (which page/element), and confirmation of the above defaults if any are wrong.
- **Starting Phase 1 (quick wins)** — no open decisions block it: #12 custom scrollbars, #31 bolder font, #20 Biodata Farm→Biodata rename, #5 nav transparency, #6 centered search bar, #22 IHN info dropdown, #27 tribe/ethnicity fields.

## 2026-07-21 (continued) — Phase 1 quick wins shipped (7/7)
- **All 7 Phase 1 items done, 250/250 tests green, type-check + lint clean.**
  - **#12 scrollbars**: `globals.css` — thin (10px) pill-shaped webkit scrollbar with a glassy amethyst gradient thumb (hover-darkens) + Firefox `scrollbar-width: thin`/`scrollbar-color` fallback.
  - **#31 bolder font**: `globals.css` — body `font-weight` 400→500, `h1`/`h2` 800 + `letter-spacing: -0.02em`, `h3`/`h4` 700 + `-0.01em`. No typeface change. Checked Button/Card/Header — all have their own explicit font-weight on the relevant text (brandName, buttons), which takes precedence over the global heading rule by specificity, so no contrast/overflow regression.
  - **#20 rename**: "Biodata Farm" → "Biodata" in `DashboardClient.tsx` heading + `docs/pages/dashboard.md`. Left `docs/research/build-public.md`'s occurrences alone — that file is a verbatim-quote summary of the founder's original raw BUILD doc (historical/archival, not a live page spec), and `SPEC.md`/`CHANGELOG.md`/`PLAN.md` are root-level historical records, out of the requested `src/`+`docs/` sweep scope. No test asserted on the old string.
  - **#5 nav transparency**: `Header.module.css` — the non-floating `.bar` variant now rests at ~86% transparent (`rgba(101,70,235,0.14)`) and raises to the original filled violet glass (`0.82` alpha) + blur on `:hover`/`:focus-within`. **Real contrast problem caught and fixed before shipping**: white nav text over a near-transparent purple tint sitting on the site's mostly-light page backgrounds would have been unreadable at rest (calculated ~1.3:1). Fixed by adding a `--header-fg` custom property that swaps to dark ink at rest and back to white on hover/focus (brand text, hospital name, hamburger icon all consume the var). Scoped every transparency/hover rule to `.bar:not(.floating)` so the homepage's map-floating variant (a separate, already-tuned lighter style) is completely untouched.
  - **#6 centered search bar**: `Header.module.css` — `.bar` switched from flex to a 3-column grid (`minmax(0,1fr) auto minmax(0,1fr)`, named areas `left search right`), so the search bar is genuinely centered regardless of `.left`/`.right` content width (fixes the old flex assumption that broke on hospital-profile pages with a logo in `.left`). Re-tuned the 900px breakpoint to a 2-col + wrapped-search-row grid-area layout (same visual behavior as before, now grid-based); 640px breakpoint (hide brand/hospital name text) untouched.
  - **#22 IHN dropdown**: `IHNCodeDisplay.tsx`/`.module.css` — added a `<details>` "Why does this matter?" collapsible with founder-appropriate copy (faster triage, safer treatment, works if you can't speak for yourself, you stay in control) styled with the existing glass tokens (`--glass-bg`/`--glass-blur`/`--glass-border`/`--shadow-sm`) matching `Card.module.css`'s register — no new visual language invented.
  - **#27 tribe/ethnicity**: confirmed `religious_status` was already NOT in `BioDataForm`'s `validate()` required-fields list (already optional, as the plan assumed). Added optional `tribe?`/`ethnicity?` to `BiodataLayer` (`src/types/index.ts`) + matching `Input` fields in `BioDataForm.tsx`. Checked `sanitizeLayer` (`lib/sanitize.ts`) — it's a generic string-field sanitizer with no fixed per-key allowlist (unlike `sanitizeClinicalConditions`, which is a whitelisted-array sanitizer for a different field), so no change was needed there; the clinical_conditions bug class does NOT repeat here.
- **Commit pushed.** Remaining WORKLIST.md items are Phase 2+ (bug repro, large features, doctor-consent workflow) — out of scope for this session per the founder's explicit "no scope creep" instruction.

## 2026-07-22 — #2 ratings/rankings on hospital profile shipped
- **Worklist #2 done: region + national rating rank on the hospital profile, "world" tier dropped for v1** (single-country/Nigeria app — matches the standing recommendation already recorded in PLAN-worklist.md).
- New `GET /api/hospitals/[id]/ranking` — kept as its OWN endpoint rather than folded into `GET /api/hospitals/[id]`, deliberately: the base profile route must stay a cheap single-row lookup on every page view, while ranking runs window functions over every `status='approved'` hospital (city-partitioned + global). Single SQL query in `src/lib/hospital-ranking.ts` (`RANKING_QUERY`): `RANK() OVER (PARTITION BY city ORDER BY rating_avg DESC, rating_count DESC, name ASC)` for region, same ordering with no partition for national, both in one pass (no N+1) via a `LEFT JOIN` so a non-approved or non-existent hospital degrades gracefully (`region: null, national: null`, still 200) instead of erroring.
- Region ranking groups by the hospital's own free-text `city` column — documented in code as a known data-quality caveat (typos/variants fragment groups); no normalized region/state field exists to fix this properly yet, intentionally not solved in this pass.
- New `HospitalRankingPanel` component in the hospital-profile top-right hero row (beside the gallery), reusing the existing `Stars` component + `Card.module.css` glass tokens (no new visual language). Zero-review hospitals show "Not yet rated" instead of a nonsensical "#1 of 1".
- Tests added: pure-function test for `toHospitalRanking`/`RANKING_QUERY` shape, a mocked route test (ranked/unranked/404/malformed-id), a component test for `HospitalRankingPanel` (normal/zero-review/no-city/unranked), and 2 integration cases in `HospitalProfileClient.test.tsx`. **294/294 tests green under `--runInBand`** — the default parallel `npm test` run showed a handful of timeout flakes that differed run-to-run (bcrypt/BioDataForm/HospitalProfileClient), confirmed as pre-existing CPU-contention flakiness (matches the already-documented `dev-tertiary.test.ts` bcrypt-timeout pattern), not a regression from this change. `tsc --noEmit` and `next lint` both clean.
- **Not verified against a live DB this session** — no query run against the 6 seeded hospitals; SQL correctness relies on the mocked-route + pure-function tests only. If a live check is wanted later: seed data would need varied `rating_avg`/`rating_count`/`city` values to produce a non-trivial ranking (right now several seed rows likely share defaults).

## 2026-07-26 — #18 email confirmation + welcome email shipped (Resend, no key yet)
- **Worklist #18 done.** No email infra existed at all beforehand (confirmed via grep before starting). Chose **Resend** (`resend` npm package, now a real dependency) per the pre-authorized decision in PLAN-worklist.md — no-card free tier, same anti-billing-signup reasoning as MapTiler/OSRM/Supabase Storage. **No real `RESEND_API_KEY` was available this session** (founder action, not automatable) — built entirely around that constraint.
- **`src/lib/email.ts`** is the one wrapper module for every outbound Resend call (mockable, dev-fallback lives here only): lazy-imports the `resend` SDK only when a key exists, wraps the send in an 8s `Promise.race` timeout, and NEVER throws — on a missing key it logs a structured `email_not_sent_no_api_key` warning (recipient/subject/truncated preview) and returns `{sent:false}`; on any SDK error/outage/timeout it logs `email_send_failed` and also returns `{sent:false}`. This is what lets signup succeed unconditionally regardless of Resend's health.
- **`src/lib/email-verification.ts`**: token gen/hash/validate, deliberately mirroring `sessions.token_hash`'s SHA-256 pattern in `auth.ts` (raw token only ever in the emailed link, hash only in the DB). 48h expiry (long enough to not require same-day checking, short enough to bound a leaked link). `consumeEmailVerificationToken` uses `SELECT ... FOR UPDATE` inside a transaction so a token is provably single-use even under concurrent requests — same defense-in-depth class as the advisory-lock rate limiter, per the instruction not to relax the bar set for sessions.
- **Migration `010_email_verification.sql`** (+ `.down`, applied to live Neon, verified via a throwaway check script): `users.email_verified BOOLEAN NOT NULL DEFAULT false`, backfilled `true` for all EXISTING rows (only new signups must verify — no retroactive lockout) + `email_verification_tokens` table (hashed token, `user_id` FK CASCADE, `expires_at`, `used_at` nullable for single-use, indexed on both user_id and token_hash).
- **Signup route** (`/api/auth/signup`) now creates a token + sends ONE combined welcome/confirmation email (subject "Confirm your Racoon Eye account", `src/lib/email-templates.ts`) after user creation — wrapped in its own try/catch so a token-creation or email-send failure is loudly logged but never turns a successful signup into a 500. Content mentions the IHN code lightly ("along with a personal IHN code you can view any time from your dashboard") without duplicating #19's full IHN-sharing warning, per `IHNCodeDisplay.tsx`'s existing tone.
- **New `GET /api/auth/verify-email?token=`**: public, rate-limited (`verify_email:<ip>`, 20/hr, separate bucket from signup's own limit), every outcome (success AND failure — invalid/expired/used/rate_limited) audit-logged under a new `email_verified` AuditAction. Redirects (not JSON) to a new `/verify-email` page — chosen because this route is meant to be opened directly from an email client, not fetched. New `/verify-email` page reuses `Card`/`.auth-page`/`Button.module.css` styling (no new visual language), shows a distinct message per status.
- **Blocking decision made: NON-blocking**, per the plan's recommended default — unverified accounts keep full dashboard/biodata access. `/api/biodata/me` GET now also returns `email_verified` (single JOIN added to its existing query, no extra round-trip) and `DashboardClient.tsx` shows a persistent Card banner ("Please verify your email address…") when false. No hard gate anywhere.
- **Rate-limiting**: confirmed the existing signup IP limit (`signup:<ip>`, 15/hr, already there for bcrypt-CPU-DoS) adequately bounds email-send volume too (one email per successful signup, same request) — no separate email-specific limiter was needed on the signup path itself; verify-email got its own distinct bucket since it's a different unauthenticated public endpoint.
- **Tests**: `email-verification.test.ts` (token format/hash-not-raw/expiry/used/invalid/happy-path via a mocked `FOR UPDATE` transaction), `email.test.ts` (mocks the `resend` package itself — no-key path, success path, SDK-error path, SDK-throw path, never throws), `verify-email.test.ts` (rate-limited/success/expired/used/invalid redirects + audit-log assertions), and 3 new cases added to `auth-signup.test.ts` (calls the email wrapper with the right recipient/subject/link, and survives both a thrown email-send and a thrown token-creation without failing the 201).
- **State: 375/377 tests green under `--runInBand`, `tsc --noEmit` clean, `next lint` clean.** The 2 failures are in `hospital-filters.test.ts` and confirmed (via `git status` mid-session) to belong to a DIFFERENT concurrent session actively editing `hospital-filters.ts`/`filter-search.ts`/`HomeClient.tsx`/`Header.tsx`/first-aid files for a different worklist item (looked like #8 symptom-search + #34 first-aid signs/symptoms, migration `011_first_aid_signs_symptoms` also appeared and got applied alongside `010` in the same `db:migrate` run) — not a regression from this change, left untouched as out-of-scope for this task.
- **Still owed**: a real `RESEND_API_KEY` from the founder (paste into Vercel env + `.env.local`, never into chat) before confirmation/welcome emails actually deliver in production — until then the dev-fallback logging path is what fires, which is by design. `SITE_URL` env var (new, documented in `.env.example`) also needs to be set to the real production URL on deploy so verify-links in emails aren't `localhost`.
- **Explicitly out of scope, left for #19** (per the task boundary): forgot-password flow itself, and the FULL IHN-sharing warning copy in the welcome email.

## 2026-07-26 (continued) — #23 granular IHN field-level sharing shipped (dead-path fix)
- **Worklist #23 done — the highest-blast-radius change in the punch list, since it modifies `GET /api/biodata/[userId]`'s authorization boundary.** Root cause (already known from the 2026-07-12 entry): `authorize()` required `session.user_id === paramUserId`, so the IHN-code parameter this route already accepted was completely unreachable by anyone but the owner — a relative with the correct IHN code could never actually use it. Fixed precisely as scoped: relaxed the READ path only (split into `authorizeRead`/`authorizeWrite` — PATCH stays strictly owner-only, unchanged).
- **New default-deny `sharing_prefs` JSONB column** (migration `013_biodata_sharing_prefs`, applied + column-verified on live Neon: `jsonb NOT NULL DEFAULT '{}'`). **Granularity decision:** grouped by logical section, not per-field — 9 keys: `profile` (whole ProfileLayer), `demographics` (occupation/marital_status/religious_status/tribe/ethnicity), `anthropometrics` (height/weight/waist/chest/hip/bmi), `chronic_disease`, `genotype`, `blood_group`, `disability`, `health_preferences`, `clinical_conditions`. Covers every field on `BiodataLayer`/`ProfileLayer` today so nothing can leak through an uncovered gap, while keeping the toggle UI to ~9 switches instead of ~25.
- **`src/lib/sharing-prefs.ts`** (new): `normalizeSharingPrefs` (default-deny — a key is only `true` if the stored value is LITERALLY `=== true`; unknown keys ignored) and `filterBiodataBySharingPrefs` (pure, DB/route-independent) — the latter is explicitly designed for #24/#25 (public "string tab"/QR lookup, queued next) to reuse unchanged rather than reimplementing the filter.
- **Rate limiting:** cross-user reads get their OWN two buckets, distinct from the existing owner bucket (`biodata:<userId>`, unchanged) — `biodata_shared_target:<targetUserId>` (10/hr, bounds brute-forcing one victim's code — the real threat model) + `biodata_shared_ip:<ip>` (30/hr, bounds one attacker spraying guesses across many targets), mirroring the dual per-IP/per-email bucket precedent already used by forgot-password (2026-07-26 earlier entry).
- **Audit:** new `biodata_shared_read` AuditAction distinguishes a cross-user read from the owner's own `biodata_read`; logs requester id, target id, and the ACTUAL field names returned (`fields_returned`, not just "some"); logged on BOTH success and IHN-mismatch failure — a wrong-code attempt against a real target is exactly the abuse signal this needs to catch.
- **UI:** new `SharingPrefsPanel.tsx` on `/dashboard` (below `IHNCodeDisplay`) — reuses the existing `show_doctors`/`HospitalFilters` checkbox-row pattern (no new visual language), full-object save via `PATCH /api/biodata/me` (now also accepts `sharing_prefs`, stored as a full replace not a partial merge — simplest safe semantics given the panel always submits the complete state).
- **Security posture verified via tests, not just described:** `sharing-prefs.test.ts` proves default-deny at the pure-function level (a field NOT opted in is never returned even when every OTHER field is opted in) — the field-level isolation rigor the task asked for, matching `hospital-isolation.test.ts`'s row-level precedent. New cross-user-read describe block in `biodata.test.ts` exercises the same guarantee through the REAL route (not just the pure function), plus: the dead path is provably fixed (no longer 403), distinct audit action fires on success AND failure, distinct rate-limit buckets are actually called, 429 on exhaustion. `biodata-me.test.ts` gained a case proving the owner's OWN read is completely unaffected by `sharing_prefs` (always full data, matching the task's explicit requirement).
- **State: 414/414 tests green** (`--runInBand`, up from 398), `tsc --noEmit` clean, `next lint` clean. Migration applied to live Neon dev DB, column verified via a throwaway script (deleted after, not committed).
- **Explicitly out of scope, left for #24/#25** (per the task boundary): the public "string tab" UI and QR code — those will consume `filterBiodataBySharingPrefs`/the relaxed `authorizeRead` path built here, not rebuild them.
- **Still owed (per CLAUDE.md, flagged not skipped):** this diff touches the exact authorization boundary every prior audit praised as correct — a dedicated security-auditor pass is still owed before this is considered fully signed off, same as the 2026-07-12 level-1-authority-model change was flagged for the same reason.

## 2026-07-26 (continued) — #19 forgot-password flow shipped
- **Worklist #19 done** (commit 70b9538). Migration `012_password_reset_tokens.sql` (+`.down`, applied to live Neon dev DB, columns verified) mirrors `email_verification_tokens`'s shape but with a **45-minute expiry** (not 48h) — documented reasoning: a leaked reset token is a full account takeover, a leaked verification token only flips a boolean.
- `src/lib/password-reset.ts` mirrors `email-verification.ts`'s exact posture (crypto-random token, hashed storage, `SELECT...FOR UPDATE` single-use, SQL-side expiry) and additionally: validates the new password via the existing `validatePasswordStrength` (not forked), hashes it with the existing bcrypt helper, updates `users.password_hash`, and **deletes every session** for the user (not "all but caller's" like `PATCH /api/account/password` — there is no caller session here since the request is unauthenticated).
- `POST /api/auth/forgot-password`: enumeration-safe (every branch — hit/miss/invalid-format/rate-limited — returns the identical generic `{message}`), rate-limited on **both** per-IP and per-email buckets (email-bombing target), and the throttled path is itself audit-logged (closing the exact class of gap #18's audit flagged). `POST /api/auth/reset-password` accepts `{token, newPassword}`, audit-logs every outcome (`password_reset_requested`/`password_reset_completed` new `AuditAction`s).
- **IHN-warning placement (judgment call, item text was ambiguous):** welcome email (`buildWelcomeVerificationEmail`) gained a brief "your IHN code is static, keep it private" line. "Warned upon signing in" was interpreted as a **one-time dismissible dashboard banner shown after login** (not the login page itself, which has no user context yet) — mirrors the #18 email-verification nudge-banner pattern in `DashboardClient.tsx`, tracked via a per-user `localStorage` flag rather than new schema (a UX nudge, not an access-control decision).
- UI: `LoginForm.tsx` gained a "Forgot password?" link (login tab only) → new `/forgot-password` page; new `/reset-password` page reads the token from the URL query param. Extracted the live password-strength checklist out of `LoginForm.tsx` into a shared `PasswordStrengthMeter.tsx` (reused by reset-password) rather than duplicating the markup.
- **State: 398/398 tests green** (`--runInBand`), `tsc --noEmit` clean, `next lint` clean. A concurrent session was mid-edit on `FirstAidForm.tsx`/`FirstAidForm.module.css`/`symptom-specialty-map.ts` (different worklist item, migration 011 already applied by that session) — confirmed via `git status` before staging and left entirely untouched; only this task's own files were committed.

## 2026-07-26 (continued) — #24/#25 "string tab" + QR shipped (commit 86f9b6a)
- **Public, opt-in-only "Find by IHN" tab** — new `Header.tsx` nav entry → `/string-lookup` page (`StringLookupClient.tsx`), kept off the full-bleed map homepage. Requires a logged-in patient session, same gate #23's cross-user IHN read already requires (didn't loosen it for "public" — public means any patient, not anonymous).
- **Lookup-key decision:** `biodata.ihn_code` is `UNIQUE` (migration 001) — confirmed a code alone is a sufficient key, so built `GET /api/biodata/lookup?ihn=CODE` (resolves code→user_id) instead of requiring a userId the emergency lookup person wouldn't have. Exported `authorizeRead`/added `buildReadResult` from `[userId]/route.ts` so the lookup route calls the EXACT SAME pipeline — no parallel/weaker read path. Pre-resolution gets sibling rate-limit buckets (`biodata_lookup_code`/`biodata_lookup_ip`, same 10/hr+30/hr limits); post-resolution reuses the existing `biodata_shared_target`/`biodata_shared_ip` buckets unchanged.
- **Enumeration-safety:** new `biodata_lookup_no_match` audit action records a code matching nobody (dev-visible only) but the API response collapses "no match" and "matched with nothing shared" into the identical `{available:false}` shape — same principle as #19's forgot-password.
- **Output:** new pure `src/lib/biodata-document.ts` (`buildBiodataDocument`) groups the already-filtered result into Profile/Demographics/Anthropometrics/Chronic disease/Genotype/Blood group/Disability/Health preferences/Clinical conditions sections (omitted when empty), rendered via `Card` with a `window.print()` button + print CSS. No PDF export — explicit v1 scope call, not an oversight.
- **QR (#25) placement decision:** generated client-side (new `qrcode` dependency, no backend surface) in `IHNCodeDisplay.tsx` (dashboard) rather than only on the lookup page — encodes a deep link `/string-lookup?ihn=<code>` (not the raw code) so a scan lands on a pre-filled form; `StringLookupClient` reads that query param to complete the loop.
- Tests: `biodata-document.test.ts`, `biodata-lookup.test.ts` (session gate, both rate-limit buckets, no-match vs nothing-shared enumeration-safety, bucket-reuse assertion proving it's not a weaker parallel path), `StringLookupClient.test.tsx`. **439/439 tests green** (`--runInBand`), `tsc --noEmit` clean, `next lint` clean.
- **Still owed (flagged, not skipped):** dedicated security-auditor pass — this is the public entry point into #23's sharing model, which itself only just got adversarially audited. #29/#30 (report generation + doctor consent) remain the next phase, depending on this.

## 2026-07-26 (continued) — #29/#30 doctor report + consent workflow shipped — LAST worklist item, 34/34 done
- **The founder's 34-item punch list is now fully built.** #29 (report generation) and #30 (doctor consent) were the two remaining items, built together since #29 depends on #30's data. Both are admin-recorded/pre-authorized-default builds per PLAN-worklist.md, not doctor-self-serve.
- **#30 — consent:** new migration `014_doctor_consent_records.sql` (+`.down`, applied to live Neon, table+columns verified via a throwaway script): `doctor_consent_records` (`doctor_id` FK, `consent_status` CHECK `pending|approved|denied`, `contacted_via`, `denial_reason`, `recorded_by_dev_id`, `decided_at`, `created_at`). **A doctor with no row at all is implicitly not-consented** (default-deny, same spirit as #23's sharing_prefs) — the current status is always the MOST RECENT row (history preserved, never mutated in place, so re-contact attempts stack rather than overwrite). New `src/app/api/dev/doctor-consent/route.ts` (GET search doctor/hospital by name via the existing ILIKE+`escapeLikePattern` pattern with the doctor's latest consent folded in via `LEFT JOIN LATERAL`; POST creates a new outcome record; PATCH updates one) — gated "any developer" (primary OR secondary), matching sibling routes `/api/dev/tertiary`/`/api/dev/first-aid`, not primary-only. New audit actions `doctor_consent_recorded`/`doctor_consent_updated`. New dev-portal page `/dev/doctor-consent` (`DevDoctorConsentClient.tsx`, reuses `DevPrimary.module.css`'s existing form/list classes verbatim, no new visual language) + nav link in `DevShell.tsx`.
- **Data-model gap closed (per the task's own instruction to decide, not skip):** nothing anywhere associated a `clinical_condition` with a specific doctor — added optional `ClinicalCondition.doctor_id` (`src/types/index.ts`), whitelisted through `sanitizeClinicalConditions` (validated as a UUID shape, silently dropped otherwise — same "must whitelist or it vanishes" bug class flagged since 2026-07-12). **Deliberately did NOT build a doctor-picker UI in `BioDataForm.tsx` this pass** — the field is fully wired end-to-end (save via PATCH, read via the filtered pipeline, consumed by report generation) but a patient currently has no UI to actually select a doctor when adding a condition; flagged as an explicit follow-up, out of scope for this task's time-box. Also added migration `015_doctor_contact_info.sql` (+`.down`, applied): `doctors.contact_phone`/`contact_email` (nullable) — the `doctors` table had NO contact field at all, needed for the "name/contact" signature block.
- **#29 — report generation:** new pure `src/lib/doctor-report.ts` (`buildDoctorReport`/`resolveDoctorAttribution`) builds ON TOP of #24's `buildBiodataDocument` (never forked) — adds photo top-right (`profile_layer.profile_photo_url`, reusing #21, no new photo field), a static declaration block ("Fields marked with a doctor attribution have been confirmed with that doctor's explicit consent; fields without such marking are patient-reported and unverified"), and a signature block. New DB-touching `src/lib/doctor-consent-db.ts` (`fetchDoctorAttributionLookup`) resolves each credited doctor's latest consent status — kept separate from the pure report builder so the attribution LOGIC stays independently unit-testable with no database (mirrors the sharing-prefs.ts pure/DB-wrapper split).
- **Generation surface (my call, documented in WORKLIST.md):** extended `GET /api/biodata/lookup` (the #24 "string tab" route) to also return a `report` object built server-side from the SAME already-filtered read — never a second independent unfiltered read, per the pre-authorized scope decision. `StringLookupClient.tsx` renders it: profile photo top-right (with a render-time `safeHttpUrl` guard, defense-in-depth matching the existing pattern), the declaration, and a signature list. No separate "generate my own report" dashboard action was built — the lookup path already satisfies the item's literal ask (report FROM IHN string/QR data); flagged as a reasonable v1.1 extension if wanted.
- **THE HARD TEST GATE — this session's actual acceptance criterion, per the task's own framing.** `src/lib/__tests__/doctor-report.test.ts` proves at BOTH the pure-resolver level and the full `buildDoctorReport` level that a doctor's name/contact can never appear without an `approved` record for that EXACT doctor+field: (a) no record at all → no attribution, note surfaced ("not yet confirmed"); (b) `denied` → no attribution, denial surfaced (not silently dropped) without ever naming the doctor; (b-continued) `pending` → same; (c) `approved` → name+contact correctly shown in both the row AND the signature block. Also covers: a different doctor being approved doesn't leak into an uncredited field; mixed conditions (approved/denied/unknown in the same report) only ever attribute the approved one; a doctor credited on multiple conditions appears once in the signature list. **A second, real end-to-end version of the same gate runs through the actual route** (`biodata-lookup.test.ts`, mocked DB) — proving the wiring itself enforces it, not just the pure function in isolation.
- **Denial-surfacing decision (documented, not left implicit):** chose to surface denial/pending/no-record as a doctor-anonymous note on the field ("a doctor was asked to confirm this but declined to be credited" / "doctor input requested, not yet confirmed") rather than fully hiding that a doctor was ever involved — judged more transparent for a reader, while never leaking WHO that doctor is without consent.
- **State: 464/464 tests green** (`--runInBand`, up from 439 — 25 new: doctor-report.test.ts's hard gate, dev-doctor-consent.test.ts's CRUD route tests, updated biodata-lookup.test.ts/StringLookupClient.test.tsx cases), `tsc --noEmit` clean, `next lint` clean. Both new migrations (014, 015) applied to the live Neon dev DB, table/columns verified via a throwaway script (deleted after, not committed).
- **This is the founder's LAST punch-list item — all 34 are now Done or explicitly Blocked-on-founder** (#9 Linktree URL, #15 logo asset — unchanged, still need founder action) **or Open pending founder specifics** (#32, vague "some static info feels base"). Essentially everything buildable without further founder input is now built.
- **Still owed, flagged not skipped (per CLAUDE.md):** a dedicated security-auditor pass on this diff — it conditionally exposes doctor PII (name/contact) based on a DB-recorded consent state, the exact liability class #30 exists to prevent, and deserves the same adversarial rigor #23's sharing-prefs boundary already received. Also un-actioned: the `BioDataForm.tsx` doctor-picker UI gap noted above, and a possible future "generate my own report from my dashboard" action if the founder wants report generation beyond the IHN-lookup surface.

## 2026-07-26 (continued) — worklist #1 Tier-1 interaction-engineering pass (commit 672e7dd)
- **Executed against a prior research diagnosis** (not re-derived): the app had liquid-glass MATERIALS (blur, refraction filter, pointer-sheen, jelly-press, sliding pill) but not BEHAVIOR (interruptible springs, continuous-not-discrete effects, depth/parallax, reserved glass surface, reduced-motion, device-tier fallback). Scoped to Tier 1 only, per the research's own prioritization.
- **`--spring` replaced with a real sampled spring** (`linear()` easing, damped-harmonic-oscillator, zeta≈0.55/wn=8, ~12.6% single overshoot) — a bezier can't be interrupted mid-gesture with velocity continuity, which was the single highest-impact gap. **While auditing every `--spring` consumer, found 4 real pre-existing bugs silently killing the spring feel entirely**: `Button.module.css`'s `:active` rule had `transition: transform var(--spring) 0s` (literal 0s duration — no animation on press at all), and `LoginForm.module.css` had 3 instances of invalid/incomplete transition shorthand (`var(--spring) ease` with a stray extra timing-function token, two of them with NO duration value at all) — meaning the sliding-pill tab control and password-strength bar likely never animated with any real duration. This is plausibly the literal mechanism behind the founder's "can't feel it" complaint on the sliding pill specifically. All fixed.
- **`sheetDrag.ts`'s `resolveSheetSnap` gained an optional `velocityPxPerMs` param** (backward-compatible default 0) — a fast flick wins over raw position now; `HomeClient.tsx` tracks an exponentially-smoothed height-velocity across the drag. New tests in `sheetDrag.test.ts`.
- **`HospitalMiniProfile`'s sheen**: pointer-tracking was already continuous (per-event); the actual gap was a `0.1s ease` snap on fade — switched to `0.35s var(--spring)`; both the sheen tracking and the new map-parallax skip their per-event writes under `prefers-reduced-motion` via new `src/lib/reducedMotion.ts`.
- **One parallax/depth proof-point** (not retrofitted everywhere): while dragging the mobile bottom sheet, the fixed map layer gets a few px of counter-offset (`--map-parallax`, capped ±6px).
- **Glass surface area reduced**: `Card` (`Card.tsx`/`Card.module.css`) gained `variant="glass"|"plain"`. Kept glass: Header, homepage results panel (own CSS), Modal/popovers, IHN "why does this matter?" dropdown, Help panel, `HospitalMiniProfile` (explicit exception), hospital-profile's in-page search-results dropdown. Converted to plain (off-white/hairline/shadow-token, no blur): every other `<Card>` consumer app-wide (~23 files, ~40 usages) — full list in `docs/liquid-glass.md`, which was updated (overriding its stale "don't touch Card/HomeClient/HospitalMiniProfile" note, same override precedent as 2026-07-11's homepage rebuild).
- **`prefers-reduced-motion` added globally** (was completely absent — a hard a11y gap) — `globals.css` collapses `--spring` + zeroes all transition/animation durations; JS-driven continuous effects (sheen, scroll-darken, drag parallax) check the new `prefersReducedMotion()` helper directly.
- **Device-tier `backdrop-filter` fallback**: new `GfxTier.tsx` (mounted in `Layout.tsx`) sets `data-gfx-tier="low"` from a coarse `hardwareConcurrency`/`deviceMemory`/connection heuristic (no frame-rate probing, deliberately simple); low tier gets a cheaper capped blur and the homepage panel drops its expensive SVG-refraction filter chain in favor of plain blur.
- **No new dependencies.** `tsc --noEmit` clean, `next lint` clean, **515/515 tests green** (`--runInBand`).
- **Concurrency note**: this session ran alongside at least one other concurrent session (worklist #8/#11 two-stage triage rebuild, #32 First Aid theming) touching overlapping files (`HomeClient.tsx`/`.module.css`). Those two files ended up already committed (by the other session, under its own commit message) by the time this session went to stage its own changes — confirmed via `git diff` that the working tree already matched HEAD for those two files with this session's velocity/parallax code correctly present, so nothing was lost, but the commit attribution for that file's diff sits under commit 8c21bdd, not this session's 672e7dd. Everything else in this task's scope (Card, Button, LoginForm, globals.css, GfxTier, reducedMotion, sheetDrag, docs/WORKLIST) was verified as a clean isolated diff before staging and committed under 672e7dd.
- **Not verified live in-browser this session** (no browser/preview tool available in this pass) — relied on `tsc`/lint/tests + direct reading of compiled CSS logic. A real-eyes check of the spring feel, the flick gesture, and the parallax offset is still owed, same caveat pattern as several earlier UI passes.
- **Deferred (Tier 2/3, not forgotten)**: scroll-linked chrome shrink, content-aware shadow, View Transitions morphing, elastic overscroll, haptics.

## 2026-07-26 (continued) — that owed security-auditor pass happened, found + fixed a real CRITICAL exploit
- **The audit flagged above ran and found a genuinely serious bug, not a theoretical one:** `doctor_consent_records` recorded consent PER DOCTOR ONLY. `GET /api/hospitals/[id]` is public/unauthenticated and returns every hospital's doctor roster (id+name) — so any patient could copy a real doctor's id, PATCH it onto their OWN fabricated `clinical_conditions[i].doctor_id`, and if that doctor had EVER been approved for ANY other patient, the report generator would wrongly credit that real doctor's name/contact to the fabricated entry. Consent answered "has this doctor ever consented, period" instead of "did this doctor consent to being credited for THIS patient's record."
- **Fix: consent re-scoped to a (doctor, patient) pair.** New migration `016_doctor_consent_patient_scope.sql` (+`.down`): `patient_user_id UUID NOT NULL REFERENCES users(id)` added to `doctor_consent_records`. Made **NOT NULL with no backfill** — reasoned (and then VERIFIED, not just assumed) that no real rows existed: the table was created the same day, no seed/bootstrap script or test fixture ever inserted one, and applying the migration to the live Neon dev DB actually succeeded (would have failed loudly on a NOT NULL violation if any row existed). `src/lib/doctor-consent-db.ts`'s `fetchDoctorAttributionLookup` now takes `(doctorIds, patientUserId)`, scoping the "most recent row" lateral join on BOTH columns; its only caller (`biodata/lookup/route.ts`) passes the biodata owner's own `user_id` — never request input.
- **HIGH fix (same audit):** `GET /api/biodata/lookup` was returning the raw `clinical_conditions[i].doctor_id` UUID in `biodata_layer` regardless of consent status — any authenticated caller could cross-reference it against the public doctor roster and learn exactly which doctor was credited even when consent was denied/pending/nonexistent, defeating the report's own anonymization. Fixed by stripping `doctor_id` from every `clinical_conditions` entry at the response-building step (not the stored record).
- **MEDIUM fixes:** (1) removed `PATCH /api/dev/doctor-consent` entirely — it mutated rows in place, contradicting the table's own documented append-only "most recent row wins, never mutate history" design (confirmed the client never actually called it before removing); corrections are now a new POST row only. (2) deterministic tie-break `ORDER BY created_at DESC, id DESC` added everywhere "current status" is resolved (was non-deterministic on an exact-tie `created_at`).
- **LOW/policy fix:** `/api/dev/doctor-consent` authz raised from "any developer" to **primary-only** (`isPrimary`, mirroring `/api/dev/accounts`'s bar) — recording a doctor's real-world consent carries liability that content-editing routes don't. `DevDoctorConsentClient.tsx` now blocks non-primary devs with the same "You do not have admin access" pattern `/dev/primary` uses, and gained a patient-search-and-select step (ILIKE on `users.email`, `account_type='patient'`, reusing the doctor search's escape/pagination pattern — no dedicated patient-lookup capability existed anywhere else in the dev portal to reuse, contrary to a guess in the audit brief that `/dev/institutions` might already have one; it doesn't, it only creates tertiary accounts by typed email).
- **THE REAL PROOF, not just a mocked assertion:** `backend.integration.test.ts` (real Postgres) approves a doctor for patient A, then proves `fetchDoctorAttributionLookup` returns NO attribution when that SAME `doctor_id` is looked up for patient B (the literal exploit) while still correctly attributing for patient A — **ran green against the live Neon DB** (`npm run test:integration`, 6/6) after applying migration 016. Also added `doctor-consent-db.test.ts` (mocked-DB unit proof of the SQL/params/tie-break) and rewrote `dev-doctor-consent.test.ts` (primary-only gate incl. a secondary-gets-403 case, `resource=patients` search, POST requiring both ids, confirms no `PATCH` export) plus two new `biodata-lookup.test.ts` cases (doctor_id stripped from the response, patient-scoping wired through with the correct id).
- **State: 476/476 tests green** (`--runInBand`, up from 464), `tsc --noEmit` clean, `next lint` clean, migration 016 applied to the live Neon dev DB. **Another security-auditor re-check on THIS diff is owed** (per the task's own instruction) — flagged, not skipped, same as every prior pass in this feature's history.

## 2026-07-26 (continued) — homepage symptom search rebuilt as two-stage clinical triage (worklist #8/#11)
- **A founder message mislabeled "#11" in chat was actually about symptom triage design, not the map-routing bug that's really #11** — cross-referenced in both WORKLIST.md rows, proceeded as a refinement of #8. Founder's ask: keep symptom search simple/base, escalate anything acute/severe (self-reported pain >=7 on 0-10) straight to urgent/general care, route simple regional symptoms (eye pain -> ophthalmologist, fracture -> orthopaedics, etc.) using real clinical triage frameworks.
- **New two-stage architecture, never merged into one score.** Stage 1 = `src/lib/symptom-red-flags.ts` (new, pure, independently tested): ~17 region-agnostic red-flag symptoms (not breathing, unresponsive, seizure ongoing, uncontrolled bleeding, severe allergic reaction, stroke signs, chest pain, poisoning/overdose, active suicidal crisis, major trauma, breathless at rest, new confusion, sudden vision loss, worst headache of my life, pregnancy bleeding/abdo pain, can't pass urine/testicular pain, vomiting blood/black stools) modeled on the Manchester Triage System's general discriminators, PLUS a literal 0-10 self-reported severity slider (the founder's exact ask) — >=7 is the standard MTS pain-discriminator boundary. Any red flag selected OR severity >=7 short-circuits straight to an emergency outcome, no specialty-matching at all, evaluated before Stage 2 ever runs.
- **Stage 2 = `src/lib/symptom-specialty-map.ts` fully rewritten** into a genuinely separate, non-emergency, region-based vocabulary (~29 simple everyday symptoms across 13 body-system regions: Eyes, ENT, Respiratory, Cardiac, Musculoskeletal, Abdominal, Neurological, Skin, Urological, Gynaecological & obstetric, Psychiatric, Dental, Infectious/general). **This replaces the earlier-today build's reuse of First Aid's own `FIRST_AID_TAGS` incident whitelist for homepage search — that reuse WAS the bug** (First Aid's 13 tags are almost all emergency/incident categories, so someone with a routine sore throat or mild eye irritation had no non-emergency option). `first-aid-tags.ts` is completely untouched, still exclusively First Aid's own catalog whitelist — the two systems are now genuinely separate closed vocabularies. Every region's specialty-keyword list ends in a `general medicine`/`internal medicine` fallback (mirrors the existing pattern), and the resolver itself falls back to General/Internal Medicine if nothing resolves — a symptom search can never come back unmapped. Still closed-vocabulary only, no free-text symptom parser (a hard safety requirement from the research, not a style choice).
- **UI rebuilt:** `Header.tsx`'s symptom picker now has a severity slider + a red-flag chip section ("Is any of this happening right now?") + a region-grouped non-emergency chip list, replacing the old flat 13-tag/min-2-selected picker. `HomeClient.tsx` gained an `emergency=1` URL-driven outcome: a persistent sticky red banner ("this may be an emergency — go to the nearest hospital or emergency department now, or call 112") with a `tel:112` call affordance, no specialty ranking, just the plain nearest-hospitals list. The non-emergency notice was rewritten to the founder's exact non-diagnostic phrasing pattern: "People with symptoms like this usually see: [specialty]. This is based on which services hospitals list, not an assessment of your condition. If you think this is an emergency, call 112 or go to the nearest hospital now." No "diagnosis"/"you have"/"likely condition" wording anywhere.
- **`docs/triage-content-review.md` added** (new stub, explicitly required by the task) — flags that both whitelists (red flags + region routing) are non-clinician-authored software logic grounded in the Manchester Triage System's general discriminators/pain ruler and standard region-based referral pathways, and must be reviewed/signed off by a licensed clinician (the founder's anchor professor/CMD partner is the obvious first reviewer) before this feature is launch-ready. This is a real documented gap, not a formality.
- **No new migration, no new logging.** This is client/query-logic only — symptom selections and severity are NOT logged anywhere, deliberately: logging health-adjacent search queries against an identity would be an NDPA/privacy concern per the research, and nothing in the feature needed it to function, so it was left unbuilt rather than built-then-flagged.
- **Concurrent-session caveat (confirmed via `git status`, not guessed):** another session was simultaneously doing a large visual/glass/parallax pass touching `HomeClient.module.css`/`.tsx` (added a `--map-parallax` effect), `sheetDrag.ts`, `Card.tsx`, `BioDataForm.tsx`, and ~20 other component files, plus new `src/components/GfxTier.tsx` and `src/lib/reducedMotion.ts` (the latter's own test, `reducedMotion.test.ts`, fails — missing a `@jest-environment jsdom` pragma — a PRE-EXISTING bug in that concurrent session's own new file, not touched or introduced by this task). Verified this task's diffs in the shared files (`Header.tsx`, `hospital-filters.ts`, `symptom-specialty-map.ts`) were clean/isolated via `git diff`; `HomeClient.tsx`/`.module.css` legitimately contain BOTH sessions' changes merged in the same file (Edit tool's old_string matching preserved the concurrent parallax code untouched) — verified the merged result still compiles/lints/passes tests before committing. **Committed only this task's specific files** (`git add` with an explicit file list, not `-A`), leaving the concurrent session's other in-progress files uncommitted for that session to finish and commit itself.
- **State: `tsc --noEmit` clean, `next lint` clean, 512/512 relevant tests green** (`npx jest --runInBand`; only failure is the unrelated pre-existing `reducedMotion.test.ts` from the concurrent session, not part of this commit). Commit 8c21bdd, pushed to origin.

## 2026-07-28 — founder bug fix: session-expiry mid-action + signed-in identity display (commit 3e821b6)
- **Founder report, not a numbered worklist item:** clicking an action button (approve hospital, save hospital info, change password, etc.) after a session expired while already on a dev/hospital-portal page showed the raw `"Not authenticated."` string inline with **no path back to login** — every page's own INITIAL load was correctly guarded (`useDevGuard()` / each dashboard's own session-check fetch already redirect on 401), but every OTHER fetch inside those pages (the actual button-click actions) had zero 401 handling.
- **Fix — one shared helper, applied everywhere, not patched ad hoc.** New `src/lib/authFetch.ts`: wraps `fetch` for action calls; on a 401 it calls an `onUnauthenticated(message)` callback (wired to each component's existing error/notice state, so the user sees "Your session has expired — redirecting to sign in…" instead of a silent jump) then `setTimeout`s a redirect to `/login` (default `window.location.href`, override via `redirect` option) — works from plain `async function` handlers, not just hook bodies, since most call sites are exactly that shape. Non-401 responses pass through completely unchanged, so every existing `if (!res.ok) ...` caller pattern still works; callers just add `if (res.status === 401) return;` before parsing the body. Covered by `src/lib/__tests__/authFetch.test.ts` (401 → callback + scheduled redirect; non-401 ok/error → pass-through, no redirect).
- **Applied to every action fetch found via a full grep audit** (not just the examples named in the bug report) across `DevHospitalsClient`, `DevInstitutionsClient`, `DevPrimaryClient`, `DevFirstAidClient`, `DevDoctorConsentClient`, `HospitalDashboardClient` (all 6 tabs: info/media/hours/departments/announcements/personnel), `DashboardClient` (patient), and `SuggestionsBoard`. Each page's own initial session-check fetch (`useDevGuard`'s `/api/dev/session`, `/api/hospital/session`, `/api/biodata/me`) was deliberately left as a plain `fetch` — it already redirects correctly and is a different code path than the action calls this bug is about.
- **Identity display added** (item 3/4 of the brief): `DevShell` now takes an optional `dev` prop (`{email, access_level}`) rendered as "Signed in as {email} · {access_level}" next to the brand; every page passes its own `useDevGuard()` result through. `HospitalShell` takes `staffEmail`; sourced from a new `staff_email` field on `GET /api/hospital/session` (switched that route from `getHospitalStaff()` to `getHospitalStaffUser()` to get the email, not just ids). `DashboardClient` (patient) now shows "Signed in as {profile_layer.email}" too, for consistency across all three portals.
- **Concurrent-session hazard hit mid-task, recovered cleanly:** partway through, a parallel session's git activity **reverted all of this task's tracked-file edits back to HEAD** (confirmed via `git diff --stat` showing empty right after a prior batch of edits that had definitely applied — only my new untracked files, `authFetch.ts`/`.test.ts`, survived since there was nothing to reset them to). Redid every edit as a full-file `Write` (not `Edit`, to avoid partial-apply risk) and committed immediately after. Also discovered unrelated concurrent-session work in the tree (`Calendar.tsx`/`.module.css`, `audit.ts`, a new `reminders` migration/API/tests) — left entirely untouched and unstaged, committed only this task's explicit file list (not `-A`), same pattern as 2026-07-26's concurrent-session handling.
- **State: `tsc --noEmit` clean** (the one pre-existing unrelated error, `.next/types/app/api/biodata/[userId]/route.ts` flagging the `authorizeRead` named export, predates this session and is untouched by it — not fixed, out of scope), **`next lint` clean**, **575/581 tests green** (`npx jest --runInBand`; the 6 failures are `auth.test.ts`/`dev-tertiary.test.ts`/`password-reset.test.ts` bcrypt-driven timeouts, reproduced in isolation too — pre-existing CPU-contention flakiness in files this task never touched, not a regression). New `authFetch.test.ts` passes cleanly. Commit 3e821b6, pushed to origin.

## 2026-07-28 (continued) — patient personal reminders on the dashboard calendar (founder ask, commit d7ca31b)
- **Founder ask, not a numbered worklist item: "Allow users to set reminders on calendar."** Built on the patient's own dashboard `Calendar.tsx` (the "plain calendar for v1" from dashboard.md) — explicitly NOT `AnnouncementCalendar.tsx` (hospital announcements, untouched).
- New migration `017_reminders.sql` (+`.down`, applied to the live dev DB via `npx tsx --env-file=.env.local scripts/migrate.ts`): simple `reminders` table (`id`, `user_id` FK→users, `title`, `note` nullable, `remind_at` timestamptz, `created_at`) + a `(user_id, remind_at)` index. Deliberately not a recurring/RRULE system.
- New `src/app/api/reminders/route.ts` (GET list/POST create) + `[id]/route.ts` (DELETE) — patient-session-gated via `getPatientSession` (same pattern as `/api/biodata/me`), every query scoped to the caller's own `user_id`; DELETE uses a single `WHERE id = $1 AND user_id = $2` (not a separate ownership lookup) so a foreign id just matches zero rows → 404, never confirming existence under another account. `sanitizeText` on title (200 char cap)/note (2000 char cap, mirrors `sanitizeClinicalConditions`' per-field caps). Abuse guards: `checkRateLimit` 30 creates/hour per user, plus a 500-reminder-per-user hard cap (chosen as a generous multiple of this codebase's existing JSON-array caps like `clinical_conditions`'s 50/`departments`'s 40, sized for a table meant to grow over years of real use rather than a biodata form). New `reminder_change` audit action.
- **`Calendar.tsx` extended in place** (not forked) — fetches `/api/reminders` on mount, shows a small dot marker on any day with a reminder, and clicking a day opens a panel listing that day's reminders (with Delete) plus an add form (title, optional time, optional note). No time given → stored as local midnight, displayed as "All day" — a deliberate v1 simplification rather than adding a separate has-time column for an ask this small (documented in-code).
- **Explicit scope decision (per the task's own instruction, not an oversight):** reminders are STORED and DISPLAYED only — no push/email delivery, no background scheduler/cron. This project has no job-scheduling infrastructure, and building one is a materially larger feature than "let users set reminders on calendar." Documented in the migration file and the commit message.
- **Concurrent-session hazard again** (third time this pattern has hit, see 2026-07-26 and the entry above): mid-build, another session's git activity reverted this task's in-progress edits to `Calendar.tsx`/`.module.css` and `audit.ts` back to HEAD (confirmed via `git status`/`git diff` suddenly showing them unmodified) — the new untracked files (migration, `api/reminders/`) survived since there was nothing to reset them to. Redid the three edits from the content already in-hand and re-verified before committing. Committed only this task's explicit file list (`git add` with named paths, not `-A`), leaving the era's other concurrent uncommitted work (DevShell/HospitalShell/dev-client `.tsx` files mid-edit by another session) untouched.
- Tests: `src/app/api/__tests__/reminders.test.ts` (401 gate, 429 rate limit, title/date validation, HTML-escaping on write, per-user cap enforcement, and — mirroring `hospital-isolation.test.ts`'s rigor — a case proving a different user's DELETE against another user's reminder id hits the scoped zero-row 404 path, not a 200) + `src/components/__tests__/Calendar.test.tsx` (marker rendering, day-click reveals the panel, add-flow POSTs and renders the result, empty-title client validation, delete-flow calls DELETE and removes it from the list).
- **State: `tsc --noEmit` clean** (same one pre-existing unrelated `.next/types` `authorizeRead` error noted above, still out of scope/untouched), **`next lint` clean**, full suite **575/581 green** (`npx jest --runInBand`; the 6 failures are the same pre-existing bcrypt-timeout CPU-contention flakiness from `dev-tertiary.test.ts`/`password-reset.test.ts` — reproduced passing in isolation with a longer timeout, confirming it's environment contention from concurrent sessions, not a regression from this change). Migration 017 applied live. Commit d7ca31b, pushed to origin.

## 2026-07-29 — primary dev password reset, forgot-password diagnosis, suggestion-modal bug fix, and homepage/header/footer polish (commit f082b2f)
- **Primary dev credentials reset** via the existing `scripts/bootstrap-admin.ts` (no changes needed to the script itself) — `pr4ject4ne@gmail.com` promoted/confirmed as `account_type='developer', access_level='primary'`, password reset, all other sessions invalidated. Verified directly against the DB (bcrypt hash matches the new password) AND via a live call to `POST /api/auth/login` on the local dev server — returned 200 with `account_type: developer, access_level: primary, redirect: /dev/dashboard`. The in-app change-password route (`PATCH /api/account/password`) was also read and is logically correct (verifies current password, enforces strength, invalidates other sessions).
- **Open question, not resolvable from this session:** the founder reported still seeing "wrong credentials" on the *live* site after the reset. Everything checks out against the local `.env.local` database, and production (pr4ject4ne.vercel.app) was independently confirmed earlier to be serving the exact same seed data (identical hospital UUIDs), which strongly suggests Vercel points at the same Neon DB — but this was never directly confirmed since there's no CLI/dashboard access from this session. If the founder still can't log in on production, the next step is either a typo/retry, or the founder checking Vercel's `DATABASE_URL` env var actually matches `.env.local`'s.
- **Forgot-password root cause confirmed (not a deploy gap, a missing config value):** `RESEND_API_KEY`/`RESEND_FROM_EMAIL`/`SITE_URL` are all unset in `.env.local`; `sendEmail()` in `src/lib/email.ts` degrades gracefully (never throws) by logging instead of sending when the key is absent. Founder was walked through adding `RESEND_API_KEY` (from resend.com, free no-card tier) + `SITE_URL` on Vercel and redeploying; founder confirmed they deployed, but the live site briefly 404'd with `DEPLOYMENT_NOT_FOUND` right after — unresolved as of this entry (likely a build-in-progress or a dangling alias; founder was asked to check the Deployments tab but hasn't reported back yet).
- **Real bug found + fixed: the suggestion-tab textarea lost focus on every single keystroke** (founder-reported as "not receiving suggestions... glitchy and retracting on every key tapped"). Root cause: `Modal.tsx`'s focus-trap `useEffect` had `onClose` in its dependency array, and every caller (`SuggestionTab.tsx`) passes an inline `() => setOpen(false)` — a new function reference every re-render. Typing in the textarea re-renders the parent (new `content` state) → new `onClose` reference → effect re-fires → `panelRef.current.focus()` yanks focus off the textarea back onto the modal panel div. Verified by direct DOM inspection (`document.activeElement` moved from the `<textarea>` to the modal's panel `<div>` after exactly one keystroke). Fixed by splitting the effect: the Escape-key listener still depends on `[open, onClose]`, but the initial-focus call now only depends on `[open]` so it fires solely on the closed→open transition.
- **Second real bug, same component: the modal never reset after a successful submission.** `SuggestionTab` stays mounted for the page's whole lifetime (rendered once by `Layout`), so `status` stuck on `'sent'` forever after the first submit — reopening just re-showed "Thank you" with no way to send a second suggestion short of a full page reload. Fixed by resetting `status`/`content`/`email` in the modal's `onClose` handler.
- **Actual root cause of "I'm not receiving suggestions": there was no notification mechanism at all.** The existing route comment claimed suggestions were "routed to SUGGESTIONS_EMAIL by an out-of-band worker" — that env var and worker never existed anywhere in the codebase; suggestions were only ever visible by manually opening `/dev/dashboard`. Added `src/lib/suggestion-notify.ts`: on every suggestion submit (both the general `/api/suggestions` and hospital-scoped `/api/hospitals/[id]/suggestions` routes), emails every active `account_type='developer', access_level='primary'` user via the existing `sendEmail()` wrapper (so it inherits the same graceful-degrade-when-no-API-key behavior). Awaited (not fire-and-forget) since Vercel can kill background work after a response returns. Also added 30s polling to `SuggestionsBoard.tsx` (the dev dashboard's suggestions list) so new submissions appear without a manual reload.
- **Founder UI polish requests, all four done:** (1) `LogoLoader` now centers full-viewport (`100dvh`, was a partial `40vh` box) and defaults to a bigger size (56→96px). (2) Header logo bumped 26→34px (badge 40→48px). (3) The homepage's mobile bottom-sheet gained a third **minimized** snap state (12vh, just the grabber+heading strip) so the map can be seen almost full-screen — `sheetDrag.ts`'s `resolveSheetSnap` now returns `'minimized' | 'collapsed' | 'expanded'` instead of a boolean; reachable by dragging past the old collapsed floor or by cycling the grabber's keyboard/Enter handler (now cycles all three states, not just two). (4) Header nav shows inline links (skipping the hamburger) in a new `(orientation: landscape) and (min-width: 640px) and (max-width: 1179px)` range — phones rotated to landscape have width to spare even though they're narrow in portrait; Footer gets a stacked mobile layout (`max-width: 640px`: column direction, tighter padding, smaller link text) instead of an uneven wrap.
- **State: `tsc --noEmit` clean, `next lint` clean, 583/583 tests green** (`npx jest`; a 6-test subset — `password-reset.test.ts`, `dev-tertiary.test.ts`, `BioDataForm.test.tsx` — timed out when run concurrently with tsc+lint under CPU contention, reproduced passing cleanly in isolation, consistent with this project's known concurrent-process flakiness pattern, not a regression). Commit f082b2f, pushed to origin.

## 2026-07-29 (continued) — production build failure: noUncheckedIndexedAccess caught a real bug tsc --noEmit missed (commit b8ada28)
- **Founder forwarded the actual Vercel build-failure email** — that's what "deployment is resulting in error" meant, and it directly explains the `DEPLOYMENT_NOT_FOUND` 404 seen on the live domain: the build never completed, so there was nothing for the domain alias to point at. Not a dashboard/domain-config issue after all.
- **Root cause:** `src/lib/sheetDrag.ts`'s new `resolveSheetSnap` (added earlier this session for the minimized sheet state) ended with `return distances[0][0]` — under this project's `tsconfig.json` `noUncheckedIndexedAccess: true`, that's a real type error (array index access is possibly undefined). Fixed with `distances[0]![0]` (safe: the array always has exactly 3 entries, one per snap state).
- **Important gap: my own `npx tsc --noEmit` run earlier in this session did NOT catch this**, even though the flag is on and a fresh `npx tsc --noEmit` after the fix (and a full `npm run build`) both reproduce/confirm it correctly. Root cause of the miss not conclusively identified — didn't reproduce it failing to catch the error again once already fixed, so couldn't isolate whether it was stale `.next`/incremental-build-info cache or a race with a concurrently-running jest process at the time. **Actionable takeaway: after this kind of fix, don't trust a clean `tsc --noEmit` alone before pushing — run a real `npm run build` too**, since that's what Vercel actually runs and it's the only check that reproduced the failure reliably.
- **State:** `tsc --noEmit` clean, `sheetDrag.test.ts` 13/13 green, full local `npm run build` succeeds end-to-end (all routes compiled). Commit b8ada28, pushed to origin.

## 2026-07-29 (continued) — 11-item founder punch list (commit a098214)
- **#7 dev login access, resolved twice this session**: first reset to the founder's originally-stated password, then the founder reported still being locked out from `/dev/primary` using a DIFFERENT password (`KimAwang633$`, their name in natural order — vs the earlier `AwangKim633$` I'd set at their explicit instruction). Reset again via `bootstrap-admin.ts` to match exactly what they're currently typing, since that's clearly the intended credential now. If this recurs, the likely cause is the founder switching between the two orderings, not a code bug — the reset mechanism itself is confirmed reliable (verified via direct DB check + live login-route call earlier this session).
- **#1/#3 IHN code copy + regenerate**: `IHNCodeDisplay.tsx` got a copy-to-clipboard button and a password-gated regenerate flow, capped at once per 30 days (migration `018_ihn_code_regenerate.sql` adds `biodata.ihn_code_regenerated_at`). This is a deliberate reversal of the code's original "static, never rotates" design (see `001_initial_schema.sql`'s own comment) — swept every place that claimed the code "never changes" (LoginForm, DashboardClient banner, help-content, both welcome-email templates) since that claim is no longer true. Security review (security-auditor subagent) caught one real issue: the initial SELECT-then-UPDATE cooldown check had a TOCTOU race under concurrent requests — fixed by folding the cooldown condition into the UPDATE's own WHERE clause (atomic check-and-write), branching on `rowCount` instead of a separate read.
- **#2 password peek toggle**: centralized in `Input.tsx` (not per-form) — every password field in the app (login, signup, reset-password, change-password, IHN-regenerate confirm) got the show/hide toggle for free from one change.
- **#4 mobile overflow, real bug found and fixed**: reproduced via direct DOM measurement (`scrollWidth` vs `clientWidth`) on `/hospitals/register` at 375px — confirmed real horizontal overflow (467px vs 375px viewport). Root cause: `Input.module.css`'s `.field` had no `min-width`, so two Input fields in a flex row (the Latitude/Longitude row) refused to shrink below their own content size and forced the whole row wider than its container — a classic flex/grid `min-width:auto` gotcha. Fixed centrally (`.field { min-width: 0 }`), which should also cover the founder's separate "loose space" complaint on the profile/dashboard page if it shares the same root cause (not independently verified there — no patient test account was readily available this session).
- **#5 "AI dashes"/clunky copy**: fixed em-dashes and stale "never changes" claims in `IHNCodeDisplay.tsx`, `help-content.ts`, both email templates, `LoginForm.tsx`, `DashboardClient.tsx`. This was NOT an exhaustive site-wide sweep — only the files already being touched for other items in this list were cleaned up. A dedicated pass over the rest of the site's user-facing copy (marketing text, other component strings) is still open if the founder wants it.
- **#6 forgot-password**: root cause (missing `RESEND_API_KEY`/`SITE_URL` on Vercel) was already diagnosed and the founder said they added the env vars and redeployed — that redeploy then failed on an unrelated `noUncheckedIndexedAccess` build error (see the entry above, commit b8ada28), which is now fixed and pushed. Whether forgot-password itself now actually sends email hasn't been re-confirmed end-to-end since — needs the founder (or a future session) to actually test it against the live site now that a real deploy should succeed.
- **#8 legals + help content**: both `privacy-policy` and `terms-and-conditions` pages were thin stubs (the privacy page literally said "a full data-protection statement... is in preparation") — substantially fleshed out with accurate, honest descriptions of actual data practices (what's collected, third parties used: Neon/Resend/MapTiler, cookies, user rights) while explicitly NOT claiming formal legal/NDPA compliance certification (that still needs real legal review). `help-content.ts` gained topics for IHN regeneration, BMI/genotype/blood-group help, calendar reminders, and hospital self-registration — all real features that had shipped without corresponding help entries.
- **#9 map pin tap**: previously, `Map.tsx` accepted an `onSelectHospital` callback but `HomeClient.tsx` never actually passed one, so tapping a pin did nothing beyond showing a small name/address popup — no directions, and the route stayed pinned to whichever hospital was "nearest" forever. Fixed: tapping a pin now fetches and displays a route/ETA to THAT specific hospital (new `routeTargetId` state in `HomeClient.tsx`), and the popup shows the computed travel time when available. The founder's other #9 complaint — "inability to self refresh or filter hospital by name and rating" from the map — was NOT addressed; name/rating filtering already exists via the results panel next to the map (Header search + `HospitalFilters`), so this may need a follow-up conversation to clarify exactly what's missing (a map-specific "search this area" feature? something else?).
- **#10 blue current-location pin**: the blue "you are here" dot already existed in `Map.tsx`, but geolocation was only ever requested after an explicit "Nearest" search. Added a second, silent, mount-only geolocation request so the dot appears proactively on every homepage visit (kept separate from the existing explicit-search request so the error banner/manual-location fallback still only shows for an actual blocked "Nearest" search, not an ambient background one).
- **#11 "search glitches, doesn't work after one use"**: extensively tested (name-mode search x2, symptom-mode search x2, all via direct clicks/dispatched events) — every case correctly re-fetched and updated results with no reproducible defect. The one thing that DID fail was pressing Enter to submit via the browser automation tool's `key` action — but that also failed on the simplest possible form (email/password login) in this same harness, strongly indicating a tooling/environment limitation of the Browser pane's synthetic key-event dispatch, not a real app bug (real browsers submit forms on Enter for `type=search`/`type=text` inputs as standard behavior). Left unfixed since no real defect was found; if the founder can reproduce this on an actual device, more specific repro steps (exact device/browser, exact sequence) are needed to dig further.
- **State**: `tsc --noEmit` clean, `next lint` clean, full `npm run build` succeeds (the real Vercel-equivalent check, learned to always run this after the earlier build-failure incident), 591/591 tests green. security-auditor review passed (one medium finding, fixed before commit). Migration 018 applied to the dev DB. Commit a098214, pushed to origin.

## 2026-07-29 (continued) — round-2 founder punch list, 15 items (commit 407eb33)
- **#3 "search isn't yielding results" — traced to Browser-pane tooling flakiness in THIS session, not an app bug.** Extensive reproduction: a plain `form_input` value-set on the text input silently failed to notify React's controlled state (no `dispatchEvent`), and separately the `computer` click/type tool stopped reliably focusing elements partway through this long session. Confirmed the actual app is correct via a fully JS-driven interaction (native setter + `dispatchEvent('input')` + `.click()`): search works perfectly. Most likely real-world explanation for the founder's report: they were testing during the build-failure/`DEPLOYMENT_NOT_FOUND` outage window (see the b8ada28 entry above), when the whole site was down.
- **#2 map pin color**: `Map.tsx` no longer auto-highlights the "nearest" pin — only `selectedHospitalId` (driven by `HomeClient.tsx`'s `routeTargetId`, set by tapping a pin) renders green (`COLOR_SELECTED`). Every other pin stays the default amethyst regardless of distance/rank.
- **#15 mobile overflow, second real instance found**: the min-width:0 fix from the previous round only covered `Input.module.css`'s `.field`; `BioDataForm.module.css`'s `.photoControls` (the profile-photo upload row: circular preview + native file input) had the exact same missing-min-width gotcha, still causing real horizontal scroll on `/dashboard` specifically (confirmed via direct DOM measurement with a throwaway test patient account, cleaned up after). Fixed the same way (`min-width: 0` + `max-width: 100%` on the file input itself, since native file inputs have unusually wide intrinsic content).
- **#5 real IHN-paste bug confirmed and fixed**: `ihn-code-format.ts`'s `LEADING_PREFIX` regex only stripped ONE leading "IHN-" occurrence. Since the input already displays "IHN-" before the user pastes anything, pasting a full code produces a raw "IHN-IHN-XXXX-..." value — only the first prefix was removed, leaving "IHN-" + garbage behind. Fixed with a repeating group (`/^(\s*IHN[-\s]*)+/`); confirmed no ReDoS risk (security review) since each repetition requires the literal "IHN" substring, no ambiguous nested quantifiers.
- **#8 tertiary accounts**: `POST /api/dev/tertiary` now accepts an optional dev-chosen `password` (validated via `validatePasswordStrength`, same as everywhere else), falling back to the original auto-generate only if omitted. `DevInstitutionsClient`'s create form now has both Email and Password fields, matching the founder's exact ask.
- **#6 doctor consent — added evidence fields, not a self-serve portal**: migration 019 adds `doctor_email`/`doctor_signature` to `doctor_consent_records`. This stays inside the existing, deliberately-scoped "admin-recorded, out-of-band" model from migration 014 (there is still no doctor login/account concept — a real doctor e-signature system remains explicitly out of scope per that migration's own documented decision) — `doctor_signature` is free text for the recording developer to paste/note what evidence of consent they received (an email reply, a signed form), not a cryptographic signature.
- **#1 suggestions**: (a) verified via server logs that the notification code added last round DOES fire correctly (attempted send to the primary dev's email, degraded gracefully since RESEND_API_KEY is still unset locally) — the founder's "not receiving" report is very likely explained by the same Resend-config + build-failure timeline as forgot-password, not a bug in the notify path itself. (b) `SuggestionTab` now auto-uses a signed-in patient's own session email (via `useSession()`, server-verified, not client-spoofable) and REQUIRES an email from a signed-out visitor (was optional before) — "must sign in or add a return email," per the founder's exact wording.
- **#4 more em-dash cleanup**: fixed in `BioDataForm.tsx`, `ResetPasswordForm.tsx`, `AnnouncementForm.tsx` (dropdown option labels), `SharingPrefsPanel.tsx`, `DevDoctorConsentClient.tsx`. NOT an exhaustive site-wide sweep — still only files already being touched, same caveat as the previous round.
- **#9 dev pages mobile sizing**: `DevShell.module.css`'s nav bar (6 links + a logout button) had zero mobile handling — no `flex-wrap`, would run straight off the right edge of the screen. Added wrap + a `max-width: 640px` breakpoint shrinking gaps/font-size. Confirmed via direct DOM measurement on `/dev/primary` at 375px: no more overflow.
- **#11 unselectable UI chrome**: added a scoped (not blanket-body) `user-select: none` rule in `globals.css` targeting `button, nav, [role='button'], .re-pin, label` — actual content (biodata, first-aid text, form input values) stays selectable; security-reviewed to confirm no copyable value (IHN code, temp passwords) lives inside an affected `<label>`.
- **#7**: added 11 more first-aid tags (Drowning, Electric shock, Fainting, Diabetic emergency, Eye injury, Nosebleed, Dental injury, Pregnancy emergency, Foreign object, Stroke, Chest pain).
- **#14**: dashboard header now reads "Hello, {full_name or email}" instead of "Dashboard" + a separate "Signed in as" line.
- **#12 "make it secure and neat"**: addressed via the security-auditor pass on this round's diff (clean — no issues found) plus the correctness fixes above; not a standalone action item.
- **#10 (audit log grouped per-user) and #13 (a tertiary tab to their hospital dashboard) were NOT implemented this round** — #10 is a moderate-size UI change to the existing `/dev` audit log page (grouping/re-rendering, not yet scoped); #13's exact meaning is ambiguous (tertiary accounts are a distinct `hospital_staff` account type already routed straight to `/hospital/dashboard` on login — there's no existing "profile" of theirs to add a tab to, unlike a patient/developer account) and needs founder clarification before implementation to avoid building the wrong thing.
- **State**: `tsc --noEmit` clean, `next lint` clean, full `npm run build` succeeds, 596/596 tests green (the same `dev-tertiary.test.ts` CPU-contention flakiness reproduced passing cleanly in isolation). security-auditor review clean (no fixes needed). Migration 019 applied to the dev DB. Commit 407eb33, pushed to origin.

## 2026-07-29 (continued) — #10 and #13 clarified and implemented (commit 9a2a020)
- **#10 "just make it work"**: `/api/dev/audit-logs` now LEFT JOINs `users.email` into each row; `DevPrimaryClient.tsx`'s audit log section groups rows by `user_id` (a "System / unknown" bucket for null-user rows, e.g. entries whose user was later deleted) and renders each user's own log list, groups ordered by most-recently-active. Verified live against real data (105 real audit rows) — groups correctly separated pr4ject4ne@gmail.com / graffitee700@gmail.com / system-level entries.
- **#13 clarified by founder**: "Make a Section tab in the top (only in tertiary accounts) to link a page for institution updates." `HospitalShell.tsx` (the shell wrapping every hospital_staff/tertiary page — there's no other consumer, so "only tertiary" was just describing scope) gained a nav link "Institution updates" → `/hospital/dashboard?tab=announcements`; `HospitalDashboardClient.tsx` now reads `?tab=` on mount to preselect that tab (wrapped its page.tsx in Suspense, required for `useSearchParams`). Verified end-to-end with a throwaway tertiary test account (created, tested, deleted — also had to manually revert `hospitals.verified` back to `false` afterward since deleting the test user only auto-nulled `account_id` via `ON DELETE SET NULL`, not the separate `verified` boolean, which would have left a real seed hospital in a decorative-verified-but-unowned state if left uncorrected).
- Also fixed: neither `HospitalShell.module.css` nor `DevShell.module.css`'s bar had ever had a working `.nav` style for HospitalShell specifically (it had none at all before this) — added alongside the new link.
- **State**: `tsc --noEmit` clean, `next lint` clean, full `npm run build` succeeds, 596/596 tests green. Commit 9a2a020, pushed to origin.
