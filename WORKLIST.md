# Founder work list — 2026-07-21

Raw list as given by founder, verbatim (numbering preserved for cross-referencing in commits/PLAN). Status column updated as items are scoped/built.

| # | Item | Status |
|---|------|--------|
| 1 | Couldn't see the smooth, fluid, water-like transition/effect (liquid-glass) | Open — needs live visual verification first (browser pane has been unreliable; may need founder to confirm what's actually rendering) |
| 2 | Rankings, ratings, # of reviews, and rating breakdown (region/national/world) on hospital profile top right | Done — **"world" tier dropped for v1** (single-country/Nigeria app, no data model maps to it yet; region + national only, per the standing recommendation in PLAN-worklist.md). New `GET /api/hospitals/[id]/ranking` (kept separate from `GET /api/hospitals/[id]` deliberately — the base profile route stays a cheap single-row lookup on every page view; ranking runs window functions over all `status='approved'` hospitals and is fetched client-side as its own request, so it's easy to later cache/recompute on a schedule without touching the profile route). Single SQL query (`src/lib/hospital-ranking.ts`'s `RANKING_QUERY`) computes region rank (via `RANK() OVER (PARTITION BY city ORDER BY rating_avg DESC, rating_count DESC, name ASC)`) and national rank (same ordering, no partition) plus group totals in one pass — no N+1. Region = grouped by the hospital's own free-text `city` column (documented caveat: typos/variants fragment groups; no normalized region field exists yet, not solved here). Only `status='approved'` hospitals are ranked/rankable; a non-approved target hospital gets `region: null, national: null` back gracefully. New `HospitalRankingPanel` component (top-right hero row next to the gallery on `HospitalProfileClient`) shows star average + review count (reusing existing `Stars`) and "#N of M" for each tier, using the existing `Card.module.css` frosted-glass tokens; zero-review hospitals show "Not yet rated" instead of a nonsensical "#1 of 1". Tests: `hospital-ranking.test.ts` (pure `toHospitalRanking` conversion + query shape), route test (`hospital-ranking.test.ts` under `api/__tests__`, mocked db, covers ranked/unranked/404/malformed-id), `HospitalRankingPanel.test.tsx` (zero-review + normal + no-city + not-ranked cases), and 2 integration cases in `HospitalProfileClient.test.tsx`. 294/294 tests green (`--runInBand`; the default parallel run showed a handful of unrelated timeout flakes that vary run-to-run — bcrypt/CPU-contention under parallel workers, a pre-existing issue this session re-confirmed, not a regression), `tsc --noEmit` clean, `next lint` clean. Not verified against a live DB this session (no live query run against seeded data) — SQL/logic verified via unit + mocked-route tests only. |
| 3 | Hamburger menu: if enough horizontal space, show contents inline on the right instead of requiring the expander (new customers should see full site offering immediately) | Done — added a new `.inlineNav` (`Header.tsx`/`Header.module.css`) rendering the same three links (Profile — reuses the existing `profileHref` logic, First Aid, Directory) as plain inline `<Link>`s in `.right`, styled with `--header-fg` (so they respect the transparent/hover states from item #5) and a hover-lift per the liquid-glass interaction rule. Gated behind a new `@media (min-width: 1180px)` breakpoint — deliberately wider than the existing 900px search-wrap breakpoint, since inline nav adds three more link-width elements to the same row as the brand+logo (left) and, on search pages, the centered 600px-max search pill; 900–1100px was too tight and would crowd/collide with the search bar, whereas 1180px clears it on common laptop/desktop widths (1280/1366/1440px+). Below 1180px the hamburger + dropdown render exactly as before (unchanged JSX/CSS, no regression to mobile/tablet). Verified: `tsc --noEmit` clean, `next lint` clean, full Jest suite 255/258 passing (the 3 failures are the pre-existing unrelated `dev-tertiary.test.ts` bcrypt-timeout flakiness noted under item #7, not touched by this change; no Header-specific test exists in the repo). Live-checked via dev server + curl (browser pane not used, per this session's known instability) — confirmed the server-rendered homepage HTML contains the new `inlineNav` markup with Profile/First Aid/Directory links, and the compiled CSS (`page.css`) contains the exact `@media (min-width: 1180px)` rule showing `.inlineNav{display:flex}` and hiding `.hamburger`/`.menu`. Did not get a real-eyes resize-the-window visual confirmation this session — DOM/CSS wiring is verified but the felt experience at the breakpoint edge is still owed a real-eyes check, same caveat pattern as items #4/#7. |
| 4 | Hospital profile picture + map: change 100vh → 80vh (founder says 100vh was a mistake — no visual cue that content continues below) | Done — scoped to homepage only (the only full-viewport-height element in the codebase; hospital-profile gallery was already an aspect-ratio box, not affected). `.stage` 100dvh→80vh so the footer's top edge peeks into view; `.mapLayer` changed from `position: absolute` (scoped to `.stage`) to `position: fixed; inset:0` so the map is a true full-viewport backdrop that stays put as the page scrolls; new `.darkenOverlay` (fixed, z-index 1, below the z-index 400 results panel) darkens progressively via a `--map-darken` CSS var driven by a rAF-throttled scroll listener in `HomeClient.tsx` (imperative DOM write, no React re-render per scroll tick, capped at 0.55 opacity). Verified no ancestor sets transform/filter/perspective/contain that would break `position: fixed`. `tsc`, lint, and full Jest suite (251 tests) all pass. Confirmed via dev server + curl that the compiled CSS matches (`.stage{height:80vh}`, `.mapLayer{position:fixed}`, `.darkenOverlay{position:fixed;z-index:1;...}`) and the page renders 200 with no server/compile errors. **Follow-up live check (same day):** with browser access, confirmed structurally in a real DOM — stage renders at exactly 80% of viewport height, `.mapLayer` is `position:fixed` and stays at `top:0` regardless of scroll, footer's top edge sits inside the viewport (peeks in as intended). Could NOT visually confirm the darken animation itself firing — this browser pane's tab runs with `document.visibilityState:"hidden"`, which suspends `requestAnimationFrame` and CSS transitions in Chromium (background-tab power-saving), so the scroll listener's rAF-scheduled updates never execute in this harness. Isolated CSS tests confirmed the `var(--map-darken)`-in-`rgba()` wiring itself resolves correctly outside the suspended-transition case, so this reads as an environment limitation, not an app bug — but the actual darken-while-scrolling behavior should still get a real-eyes check at localhost:3000 or production before calling it fully signed off. |
| 5 | Nav bar purple looks "sick and pale" — make bar 80–95% transparent except on hover (preserve legibility of bar contents at that transparency) | Done — non-floating `.bar` now ~86% transparent at rest (dark ink text/icons for contrast against light pages), raises to filled violet glass + white text on hover/focus-within; homepage `.floating` map variant untouched |
| 6 | Centralize (center) the search bar in the nav bar | Done — `Header.module.css` switched to a 3-col grid (`left`/`search`/`right`), search bar now truly centers regardless of side-column width; responsive breakpoints re-tuned (900px still drops search to its own row, now grid-area based) |
| 7 | Can't expand results on the search bar | Done — root cause: the mobile bottom-sheet grabber (`HomeClient.tsx`) was purely decorative (`aria-hidden`, no handler), so `.panel`'s `max-height: 58%` (of `.stage`, which shrank 100vh→80vh for item #4 the same day, silently shrinking the effective sheet from 58vh to ~46.4vh) was a hard ceiling users could never lift. Wired real Pointer Events drag-to-resize on the grabber (`onPointerDown`/`pointermove`/`pointerup` + `setPointerCapture`, mouse+touch+pen in one handler set, mobile/<900px only — desktop's 392px docked side panel is untouched); height is written imperatively to the DOM while dragging (no per-frame re-render, same pattern as the existing scroll-darken effect) and clamped between a collapsed floor and expanded ceiling via new pure helpers in `src/lib/sheetDrag.ts`. On release, snaps to whichever state (collapsed/expanded) is nearer via a CSS `height` transition using the existing `--spring` easing token. Snap points: collapsed = **46vh** (fixed viewport-relative, not `.stage`-relative — deliberately not `58%` of `.stage`, since that's the value that already silently shrank once when `.stage` changed; pinning to `vh` means a future `.stage` height change can't quietly shrink the sheet again), expanded = **88vh** (a clear majority of the viewport, short of 100vh so a sliver of map stays visible, preserving item #4's "there's more below" cue). Grabber is now a real control: `role="button"`, `tabIndex={0}`, `aria-expanded`, `aria-label` toggling "Expand results"/"Collapse results", Enter/Space toggles collapsed↔expanded, `aria-hidden` removed. Verified: `tsc --noEmit` clean, `next lint` clean, full Jest suite green (258/258 incl. 7 new unit tests for the pure clamp/snap math in `src/lib/__tests__/sheetDrag.test.ts` — the 3 `dev-tertiary.test.ts` timeouts seen in one run are pre-existing/unrelated bcrypt-perf flakiness, not touched by this change). Did not get a live-browser visual confirmation of the drag gesture itself this session (no browser/preview tool available in this pass) — the DOM/CSS wiring and snap math are verified, but the actual felt drag experience on a touch device is still owed a real-eyes check, same caveat pattern as item #4's follow-up note. **Code-review fix-up (same day):** `.panel.expanded`'s 88vh height rule had higher CSS specificity than the desktop media query's `.panel` override, so it could pin the desktop docked side panel to 88vh if `sheetExpanded` state ever carried across a resize/breakpoint crossing — scoped the rule inside `<900px`. Also hardened `onGrabberPointerDown` against a second concurrent pointer orphaning the drag state. Both re-verified: `tsc`, lint, full suite green. |
| 8 | Symptom search should actually work and link to hospitals ranked by price, location, availability (closest date/time), and general rank | Open — significant feature, ranking algorithm needed |
| 9 | Footer links → Linktree (URL to come later) | **Blocked on founder** — keep reminding |
| 10 | Remove doctors section for pharmacies/non-doctor institutions; verified accounts can add one if applicable | Done — new `show_doctors` boolean on `hospitals` (migration 007, backfilled by `service_type` for existing rows: hospital/clinic default true, pharmacy/radiology/other default false), editable per-hospital via the portal's Info tab checkbox (`PATCH /api/hospital/[id]/info`, same ownership-guard pattern as sibling fields). `HospitalProfileClient` only renders `DoctorRoster` when `show_doctors && doctors.length > 0`. Public GET routes updated to return the field. Migration applied to dev DB. The builder that started this crashed mid-task on a transient API connection error (not a logic bug) — resumed and finished the review myself: fixed one test bug it left behind (a `getByText` query that broke once the hospital name legitimately appears twice — header brand + page heading — switched to `getAllByText`), verified the rest of its work (migration reasoning, route/portal diffs) was correct as-is. 278/278 tests green, tsc/lint clean. |
| 11 | Fix the routing system (map) | **Blocked — no repro found.** Debugger pass (2026-07-21) checked `Map.tsx`'s route `fitBounds`, `lib/map.ts`'s OSRM fetch/response handling, the manual-location fallback, and CSP allowlisting — all read correctly, and the public OSRM demo server responds fine. A live check on the deployed site was attempted but blocked by this session's browser being unreliable (unrelated instability, not app evidence). Founder told to proceed without it for now; needs a concrete symptom description (no route drawn? wrong route? error shown? blank map?) before this can be scoped further. |
| 12 | Custom scrollbars (default ones look tacky) | Done — site-wide thin, pill-shaped, glassy-gradient scrollbar (`globals.css`, `--color-amethyst`/`--color-blue-light` tokens), hover-darken; Firefox `scrollbar-width`/`scrollbar-color` fallback |
| 13 | Homepage hospital side panel: filters by distance (km radius), private vs public, rating, day of week | Open — new feature |
| 14 | Verified accounts can add/organize services by department, sub-classifiable as they see fit | Done — new JSONB `departments` column on `hospitals` (migration 008, `[{name, services: string[]}]`, consistent with the existing `hours`/`photos` JSONB convention rather than a child table — free-form `services` per department satisfies "subclassed as they see fit" without a rigid taxonomy). Public GET routes (`/api/hospitals`, `/api/hospitals/[id]`) return it. Verified-hospital write path: `PATCH /api/hospital/[id]/info` accepts `departments`, validated/sanitized via new `sanitizeDepartments` (`src/lib/sanitize.ts` — escapes name/services, drops entries with no name, caps 40 departments × 40 services, 200 chars each, mirroring `specialties`'/`sanitizeClinicalConditions`'s existing caps). Dashboard gained a "Departments" tab (`HospitalDashboardClient.tsx`) — add/rename/remove department, add/remove services within it, bulk-saved like the existing Hours tab. Public profile gained a `HospitalDepartments` display component (collapsible, `Card.module.css` glass tokens, pill-styled service badges matching `DoctorRoster`'s visual language), rendered on `HospitalProfileClient`. Migration applied to the dev DB (Neon). Tests: `sanitizeDepartments` unit tests, `hospital-isolation.test.ts` PATCH accept/reject + cross-hospital 403 cases, `HospitalProfileClient` display tests. 306/306 tests green (`--runInBand`, no flakes this run), `tsc --noEmit` + lint clean. |
| 15 | Logo change (founder will provide the new logo asset later) | **Blocked on founder** — keep reminding |
| 16 | Direct question: "panda eye or raccoon eye?" statistically/objectively | Answer directly, no build needed |
| 17 | Password creation instructions + green/red-light validation (strength indicator) | Open — new feature |
| 18 | Email confirmation system + welcome email for new users | Open — significant feature (needs email delivery infra) |
| 19 | Forgot-password flow, secure + functional; requires email confirmation; warn users about their unique IHN string at signup and in the welcome email | Open — significant feature, depends on #18 |
| 20 | Rename "Biodata Farm" → "Biodata" everywhere | Done — dashboard heading + `docs/pages/dashboard.md` updated; no test asserted on the old string |
| 21 | No arrangement for a profile photo | Done — added `profile_photo_url?: string` to `ProfileLayer`; reused the existing `uploadFile`/`/api/uploads` plumbing (no new upload route) with a minimal single-image control in `BioDataForm.tsx`'s Profile section (circular preview, upload/remove, frosted-glass row matching `Card.module.css` tokens); both `biodata/me` and `biodata/[userId]` PATCH routes re-validate the URL with `safeHttpUrl` (sanitizeLayer only escapes HTML, doesn't check scheme) so a `javascript:`/`data:` value is dropped instead of stored. Verified round-trip (save → reload) via new route + component tests. **Clarified (same day):** "Remove photo" is deliberately display-only — founder confirmed server data should never be deleted by this action, only what's shown on the frontend changes. A code-review pass had flagged the opposite (that removal silently failed to clear the server-side value) and a fix was applied to make it actually clear on the server; reverted per founder direction, and the regression test now asserts the intended behavior (outgoing payload omits the key; server value stays untouched). |
| 22 | Add a dropdown explanation of why [biodata/IHN] is important | Done — collapsible "Why does this matter?" added to `IHNCodeDisplay`, frosted-glass treatment matching `Card.module.css`'s glass tokens |
| 23 | Users should be able to select what is visible through the IHN link (granular field-level sharing controls) | Open — significant feature, ties into the long-standing "IHN sharing is a dead path" item from memory |
| 24 | Homepage "string" tab: lets people look up a user's IHN string for biodata extraction, output as an organized document; must be extensively logged, dev-accessible | Open — significant feature |
| 25 | QR code accessible on the string tab | Open — depends on #24 |
| 26 | Footer help bar: detailed instructions organized by function/procedure, with its own keyword search | Done — added a "Help" link in `Footer.tsx` that opens the existing `Modal.tsx` as a help panel (`HelpBar.tsx`); content is static structured data (`src/lib/help-content.ts`, `{section, topic, answer}[]`) covering search modes, IHN code, Biodata, First Aid, and suggestions; client-side substring search filters across all three fields; styled with the existing glass tokens (`Card.module.css` pattern). Chose a modal over a `/help` page since Modal already provides Escape/focus handling and the content fits comfortably without full-page navigation. New test `HelpBar.test.tsx`. |
| 27 | Religion, tribe, ethnicity should be optional | Done — confirmed `religious_status` was already optional (not in `BioDataForm` validation); added optional `tribe`/`ethnicity` to `BiodataLayer` + form inputs; `sanitizeLayer` is a generic string sanitizer (no fixed allowlist) so no save-path change needed |
| 28 | Chronic disease section needs slots: disease, duration, progression, complication, care, cause (recommend doctor aid) | Done — extended `ClinicalCondition` with `duration?`/`progression?`/`complication?`/`care?` (disease=`condition`, cause already existed); added the four optional inputs to the add-condition sub-form in `BioDataForm.tsx` plus a "recommend doctor aid" helper note (copy only, no workflow); **critically whitelisted the 4 new keys in `sanitizeClinicalConditions` (`src/lib/sanitize.ts`)** — this is the exact silent-drop bug class flagged from the 2026-07-12 `cause`/`timestamp` addition, verified via a save/reload round-trip test in `biodata-me.test.ts`, not just rendering. |
| 29 | Build a system to generate a report from IHN string/QR data — neatly organized, photo top-right, declaration + doctor signature block (name/contact) where doctor-recommended fields were approved | Open — large feature, depends on #23/#24/#30 |
| 30 | Doctor must explicitly consent to use of their name/info; doctor must be contacted and consent verified before approval; space to record denial of consent | Open — large feature, compliance-sensitive |
| 31 | Bolder/thicker/sharper-edged font | Done — base body weight 400→500, headings 700/800 with tightened letter-spacing (`globals.css`); same system-ui stack, no typeface swap; checked Button/Card/Header, no overflow/contrast regressions (component-level font-weight overrides take precedence) |
| 32 | Some static info feels "base"/out of place — needs a content/design pass | Open — needs founder to point at specifics |
| 33 | First aid entries don't feel intuitive; no space for pictures; keep simple but make intuitive | Open — UX rework |
| 34 | Add signs/symptoms section before procedure in first-aid entries (so people can tell if indicated); symptom search bar should require min. 2 symptoms to narrow to the right entry | Open — schema + search algorithm change |

## Standing reminders owed to founder
- **#9** — need the Linktree URL for footer links.
- **#15** — need the new logo asset.

## Direct question to answer now
- **#16** — panda eye vs raccoon eye (see chat response): raccoon eye, no contest — stronger/more distinctive brand hook, already built.

## Phased execution plan
See [PLAN-worklist.md](PLAN-worklist.md) for the full phased plan (planner pass, 2026-07-21) — recon findings, phase groupings, parallelization, and open decisions. Update this file's Status column as items land; keep the raw list above unedited.
