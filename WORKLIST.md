# Founder work list — 2026-07-21

Raw list as given by founder, verbatim (numbering preserved for cross-referencing in commits/PLAN). Status column updated as items are scoped/built.

| # | Item | Status |
|---|------|--------|
| 1 | Couldn't see the smooth, fluid, water-like transition/effect (liquid-glass) | Open — needs live visual verification first (browser pane has been unreliable; may need founder to confirm what's actually rendering) |
| 2 | Rankings, ratings, # of reviews, and rating breakdown (region/national/world) on hospital profile top right | Done — **"world" tier dropped for v1** (single-country/Nigeria app, no data model maps to it yet; region + national only, per the standing recommendation in PLAN-worklist.md). New `GET /api/hospitals/[id]/ranking` (kept separate from `GET /api/hospitals/[id]` deliberately — the base profile route stays a cheap single-row lookup on every page view; ranking runs window functions over all `status='approved'` hospitals and is fetched client-side as its own request, so it's easy to later cache/recompute on a schedule without touching the profile route). Single SQL query (`src/lib/hospital-ranking.ts`'s `RANKING_QUERY`) computes region rank (via `RANK() OVER (PARTITION BY city ORDER BY rating_avg DESC, rating_count DESC, name ASC)`) and national rank (same ordering, no partition) plus group totals in one pass — no N+1. Region = grouped by the hospital's own free-text `city` column (documented caveat: typos/variants fragment groups; no normalized region field exists yet, not solved here). Only `status='approved'` hospitals are ranked/rankable; a non-approved target hospital gets `region: null, national: null` back gracefully. New `HospitalRankingPanel` component (top-right hero row next to the gallery on `HospitalProfileClient`) shows star average + review count (reusing existing `Stars`) and "#N of M" for each tier, using the existing `Card.module.css` frosted-glass tokens; zero-review hospitals show "Not yet rated" instead of a nonsensical "#1 of 1". Tests: `hospital-ranking.test.ts` (pure `toHospitalRanking` conversion + query shape), route test (`hospital-ranking.test.ts` under `api/__tests__`, mocked db, covers ranked/unranked/404/malformed-id), `HospitalRankingPanel.test.tsx` (zero-review + normal + no-city + not-ranked cases), and 2 integration cases in `HospitalProfileClient.test.tsx`. 294/294 tests green (`--runInBand`; the default parallel run showed a handful of unrelated timeout flakes that vary run-to-run — bcrypt/CPU-contention under parallel workers, a pre-existing issue this session re-confirmed, not a regression), `tsc --noEmit` clean, `next lint` clean. Not verified against a live DB this session (no live query run against seeded data) — SQL/logic verified via unit + mocked-route tests only. |
| 3 | Hamburger menu: if enough horizontal space, show contents inline on the right instead of requiring the expander (new customers should see full site offering immediately) | Done — added a new `.inlineNav` (`Header.tsx`/`Header.module.css`) rendering the same three links (Profile — reuses the existing `profileHref` logic, First Aid, Directory) as plain inline `<Link>`s in `.right`, styled with `--header-fg` (so they respect the transparent/hover states from item #5) and a hover-lift per the liquid-glass interaction rule. Gated behind a new `@media (min-width: 1180px)` breakpoint — deliberately wider than the existing 900px search-wrap breakpoint, since inline nav adds three more link-width elements to the same row as the brand+logo (left) and, on search pages, the centered 600px-max search pill; 900–1100px was too tight and would crowd/collide with the search bar, whereas 1180px clears it on common laptop/desktop widths (1280/1366/1440px+). Below 1180px the hamburger + dropdown render exactly as before (unchanged JSX/CSS, no regression to mobile/tablet). Verified: `tsc --noEmit` clean, `next lint` clean, full Jest suite 255/258 passing (the 3 failures are the pre-existing unrelated `dev-tertiary.test.ts` bcrypt-timeout flakiness noted under item #7, not touched by this change; no Header-specific test exists in the repo). Live-checked via dev server + curl (browser pane not used, per this session's known instability) — confirmed the server-rendered homepage HTML contains the new `inlineNav` markup with Profile/First Aid/Directory links, and the compiled CSS (`page.css`) contains the exact `@media (min-width: 1180px)` rule showing `.inlineNav{display:flex}` and hiding `.hamburger`/`.menu`. Did not get a real-eyes resize-the-window visual confirmation this session — DOM/CSS wiring is verified but the felt experience at the breakpoint edge is still owed a real-eyes check, same caveat pattern as items #4/#7. |
| 4 | Hospital profile picture + map: change 100vh → 80vh (founder says 100vh was a mistake — no visual cue that content continues below) | Done — scoped to homepage only (the only full-viewport-height element in the codebase; hospital-profile gallery was already an aspect-ratio box, not affected). `.stage` 100dvh→80vh so the footer's top edge peeks into view; `.mapLayer` changed from `position: absolute` (scoped to `.stage`) to `position: fixed; inset:0` so the map is a true full-viewport backdrop that stays put as the page scrolls; new `.darkenOverlay` (fixed, z-index 1, below the z-index 400 results panel) darkens progressively via a `--map-darken` CSS var driven by a rAF-throttled scroll listener in `HomeClient.tsx` (imperative DOM write, no React re-render per scroll tick, capped at 0.55 opacity). Verified no ancestor sets transform/filter/perspective/contain that would break `position: fixed`. `tsc`, lint, and full Jest suite (251 tests) all pass. Confirmed via dev server + curl that the compiled CSS matches (`.stage{height:80vh}`, `.mapLayer{position:fixed}`, `.darkenOverlay{position:fixed;z-index:1;...}`) and the page renders 200 with no server/compile errors. **Follow-up live check (same day):** with browser access, confirmed structurally in a real DOM — stage renders at exactly 80% of viewport height, `.mapLayer` is `position:fixed` and stays at `top:0` regardless of scroll, footer's top edge sits inside the viewport (peeks in as intended). Could NOT visually confirm the darken animation itself firing — this browser pane's tab runs with `document.visibilityState:"hidden"`, which suspends `requestAnimationFrame` and CSS transitions in Chromium (background-tab power-saving), so the scroll listener's rAF-scheduled updates never execute in this harness. Isolated CSS tests confirmed the `var(--map-darken)`-in-`rgba()` wiring itself resolves correctly outside the suspended-transition case, so this reads as an environment limitation, not an app bug — but the actual darken-while-scrolling behavior should still get a real-eyes check at localhost:3000 or production before calling it fully signed off. |
| 5 | Nav bar purple looks "sick and pale" — make bar 80–95% transparent except on hover (preserve legibility of bar contents at that transparency) | Done — non-floating `.bar` now ~86% transparent at rest (dark ink text/icons for contrast against light pages), raises to filled violet glass + white text on hover/focus-within; homepage `.floating` map variant untouched |
| 6 | Centralize (center) the search bar in the nav bar | Done — `Header.module.css` switched to a 3-col grid (`left`/`search`/`right`), search bar now truly centers regardless of side-column width; responsive breakpoints re-tuned (900px still drops search to its own row, now grid-area based) |
| 7 | Can't expand results on the search bar | Done — root cause: the mobile bottom-sheet grabber (`HomeClient.tsx`) was purely decorative (`aria-hidden`, no handler), so `.panel`'s `max-height: 58%` (of `.stage`, which shrank 100vh→80vh for item #4 the same day, silently shrinking the effective sheet from 58vh to ~46.4vh) was a hard ceiling users could never lift. Wired real Pointer Events drag-to-resize on the grabber (`onPointerDown`/`pointermove`/`pointerup` + `setPointerCapture`, mouse+touch+pen in one handler set, mobile/<900px only — desktop's 392px docked side panel is untouched); height is written imperatively to the DOM while dragging (no per-frame re-render, same pattern as the existing scroll-darken effect) and clamped between a collapsed floor and expanded ceiling via new pure helpers in `src/lib/sheetDrag.ts`. On release, snaps to whichever state (collapsed/expanded) is nearer via a CSS `height` transition using the existing `--spring` easing token. Snap points: collapsed = **46vh** (fixed viewport-relative, not `.stage`-relative — deliberately not `58%` of `.stage`, since that's the value that already silently shrank once when `.stage` changed; pinning to `vh` means a future `.stage` height change can't quietly shrink the sheet again), expanded = **88vh** (a clear majority of the viewport, short of 100vh so a sliver of map stays visible, preserving item #4's "there's more below" cue). Grabber is now a real control: `role="button"`, `tabIndex={0}`, `aria-expanded`, `aria-label` toggling "Expand results"/"Collapse results", Enter/Space toggles collapsed↔expanded, `aria-hidden` removed. Verified: `tsc --noEmit` clean, `next lint` clean, full Jest suite green (258/258 incl. 7 new unit tests for the pure clamp/snap math in `src/lib/__tests__/sheetDrag.test.ts` — the 3 `dev-tertiary.test.ts` timeouts seen in one run are pre-existing/unrelated bcrypt-perf flakiness, not touched by this change). Did not get a live-browser visual confirmation of the drag gesture itself this session (no browser/preview tool available in this pass) — the DOM/CSS wiring and snap math are verified, but the actual felt drag experience on a touch device is still owed a real-eyes check, same caveat pattern as item #4's follow-up note. **Code-review fix-up (same day):** `.panel.expanded`'s 88vh height rule had higher CSS specificity than the desktop media query's `.panel` override, so it could pin the desktop docked side panel to 88vh if `sheetExpanded` state ever carried across a resize/breakpoint crossing — scoped the rule inside `<900px`. Also hardened `onGrabberPointerDown` against a second concurrent pointer orphaning the drag state. Both re-verified: `tsc`, lint, full suite green. |
| 8 | Symptom search should actually work and link to hospitals ranked by price, location, availability (closest date/time), and general rank | Done — **scoped to specialty-match + distance + rating for v1** (no `price` field or booking/date-time availability system exists anywhere in the schema; SPEC.md explicitly defers the clinic-booking module — inventing either would be out of scope, flagged rather than silently under-delivered). Fixed the real bug: `HomeClient.tsx`'s `loadHospitals` read `symptomQuery` off the URL and showed a notice banner for it but never added `symptom` to the `/api/hospitals` fetch, so a symptom search silently fell through to the unfiltered list — now wired through `buildHospitalsQuery`/`filter-search.ts` (the homepage's actual query builder; `hospital-filters.ts` is the server-side builder, kept separate per today's earlier #13 work). New `src/lib/symptom-specialty-map.ts` — a static, exhaustively-typed (`Record<FirstAidTag, string[]>`) whitelist→specialty-keyword lookup reusing the SAME 13-tag whitelist as First Aid's `tags`/`signs_symptoms` (`first-aid-tags.ts`), documented per-tag reasoning inline. `hospital-filters.ts`'s `buildHospitalFilters` gained a `symptoms` param: matches whitelisted tags → keyword set → `EXISTS (SELECT 1 FROM unnest(specialties) sp WHERE sp ILIKE ANY($n::text[]))`, and returns a new `orderBy` (specialty-match-count DESC, then distance ASC via the same haversine expression #13 built for the radius filter — refactored to be reusable, not duplicated — then `rating_avg` DESC, then name) that `/api/hospitals` uses in place of its default verified-first ordering when a symptom search is active. Client-side proximity re-sort in `HomeClient.tsx` is now skipped for symptom searches specifically (it would otherwise silently discard the server's match-strength ranking down to plain nearest-first). Safety disclaimer notice kept intact (still says "guidance only, never diagnoses, call emergency services"). Tests: `symptom-specialty-map.test.ts` (exhaustiveness + normalize/keyword-union), `hospital-filters.test.ts` (symptom condition/orderBy/unrecognised-tag/distance-folding cases + 2 fixed pre-existing tests whose assertions no longer matched the new lat/lng-always-bound-when-valid behavior), `HomeClient.test.tsx` (new — regression-asserts the fixed `symptom` param actually reaches the fetch URL, and the notice renders). Migration `011_first_aid_signs_symptoms` (see #34) also applied to live Neon this session. 366/366 tests green, `tsc --noEmit` + lint clean. |
| 9 | Footer links → Linktree (URL to come later) | **Blocked on founder** — keep reminding |
| 10 | Remove doctors section for pharmacies/non-doctor institutions; verified accounts can add one if applicable | Done — new `show_doctors` boolean on `hospitals` (migration 007, backfilled by `service_type` for existing rows: hospital/clinic default true, pharmacy/radiology/other default false), editable per-hospital via the portal's Info tab checkbox (`PATCH /api/hospital/[id]/info`, same ownership-guard pattern as sibling fields). `HospitalProfileClient` only renders `DoctorRoster` when `show_doctors && doctors.length > 0`. Public GET routes updated to return the field. Migration applied to dev DB. The builder that started this crashed mid-task on a transient API connection error (not a logic bug) — resumed and finished the review myself: fixed one test bug it left behind (a `getByText` query that broke once the hospital name legitimately appears twice — header brand + page heading — switched to `getAllByText`), verified the rest of its work (migration reasoning, route/portal diffs) was correct as-is. 278/278 tests green, tsc/lint clean. |
| 11 | Fix the routing system (map) | **Blocked — no repro found.** Debugger pass (2026-07-21) checked `Map.tsx`'s route `fitBounds`, `lib/map.ts`'s OSRM fetch/response handling, the manual-location fallback, and CSP allowlisting — all read correctly, and the public OSRM demo server responds fine. A live check on the deployed site was attempted but blocked by this session's browser being unreliable (unrelated instability, not app evidence). Founder told to proceed without it for now; needs a concrete symptom description (no route drawn? wrong route? error shown? blank map?) before this can be scoped further. |
| 12 | Custom scrollbars (default ones look tacky) | Done — site-wide thin, pill-shaped, glassy-gradient scrollbar (`globals.css`, `--color-amethyst`/`--color-blue-light` tokens), hover-darken; Firefox `scrollbar-width`/`scrollbar-color` fallback |
| 13 | Homepage hospital side panel: filters by distance (km radius), private vs public, rating, day of week | Done — new migration `009_hospital_ownership_type.sql` adds `hospitals.is_private BOOLEAN NOT NULL DEFAULT false` (plain boolean chosen over a CHECK enum since ownership only has two meaningful states today; existing rows explicitly backfilled to `false`/public, no signal to infer real ownership from the seeded data). `src/lib/hospital-filters.ts` (new, pure/unit-tested query-builder, mirrors `filter-search.ts`'s separation-of-concerns pattern) composes `location`/`specialty`/`service_type`/`min_rating`/`open24`/`q`/`ownership`/`open_day`/`radius_km`+lat+lng with AND logic; distance filtering uses a plain haversine SQL expression (no PostGIS extension in this project), day-of-week matches the existing `hours` JSONB's lowercase 3-letter keys (accepts both `monday` and `mon` from the API param). `src/app/api/hospitals/route.ts` wired to the new builder. New `HospitalFilters.tsx`/`.module.css` component in the homepage results panel (collapsible via a `.filterToggle` pill next to the results heading), reusing `Card`/glass tokens. Migration applied to dev DB. **Picked up mid-task**: the builder crashed on a transient API connection error before finishing verification — I resumed, found and fixed two real bugs its own tests caught: (1) a test itself had a stale expectation (`q` search params should be `%wildcarded%` for ILIKE, matching the codebase's existing search convention — the implementation was correct, the test wasn't); (2) unrelated to this item, see #17's note below for the second bug found in the same verification pass. 325/325 tests green, tsc/lint clean. |
| 14 | Verified accounts can add/organize services by department, sub-classifiable as they see fit | Done — new JSONB `departments` column on `hospitals` (migration 008, `[{name, services: string[]}]`, consistent with the existing `hours`/`photos` JSONB convention rather than a child table — free-form `services` per department satisfies "subclassed as they see fit" without a rigid taxonomy). Public GET routes (`/api/hospitals`, `/api/hospitals/[id]`) return it. Verified-hospital write path: `PATCH /api/hospital/[id]/info` accepts `departments`, validated/sanitized via new `sanitizeDepartments` (`src/lib/sanitize.ts` — escapes name/services, drops entries with no name, caps 40 departments × 40 services, 200 chars each, mirroring `specialties`'/`sanitizeClinicalConditions`'s existing caps). Dashboard gained a "Departments" tab (`HospitalDashboardClient.tsx`) — add/rename/remove department, add/remove services within it, bulk-saved like the existing Hours tab. Public profile gained a `HospitalDepartments` display component (collapsible, `Card.module.css` glass tokens, pill-styled service badges matching `DoctorRoster`'s visual language), rendered on `HospitalProfileClient`. Migration applied to the dev DB (Neon). Tests: `sanitizeDepartments` unit tests, `hospital-isolation.test.ts` PATCH accept/reject + cross-hospital 403 cases, `HospitalProfileClient` display tests. 306/306 tests green (`--runInBand`, no flakes this run), `tsc --noEmit` + lint clean. |
| 15 | Logo change (founder will provide the new logo asset later) | **Blocked on founder** — keep reminding |
| 16 | Direct question: "panda eye or raccoon eye?" statistically/objectively | Answer directly, no build needed |
| 17 | Password creation instructions + green/red-light validation (strength indicator) | Done — surfaces the existing `validatePasswordStrength`/new `getPasswordChecks` (`src/lib/validation.ts`, single source of truth shared by this UI and server-side signup/reset validation, not forked) as a live checklist on the signup tab: 5 rules (length/lowercase/uppercase/digit/symbol) each shown met (✓, green) or unmet (○) in real time as the user types, plus a 3-tier weak/fair/strong pill bar and an `aria-live="polite"` summary line, `aria-describedby` linking the password input to the checklist. Styled with `LoginForm.module.css`'s existing glass register. **Picked up mid-task** (same crash pattern as #13, transient API error): resumed and found the strength-tier threshold didn't actually match the builder's own test scenarios — `metCount <= 2 → weak` disagreed with its test expecting "fair" at exactly 2 of 5 checks met; fixed to `<=1 weak / <=3 medium / else strong`, which the existing weak→fair→strong test progression now correctly exercises. 325/325 tests green, tsc/lint clean. |
| 18 | Email confirmation system + welcome email for new users | Done — **real `RESEND_API_KEY` still needed from the founder before emails actually send in production** (no key was available to this session; the dev-fallback path logs a structured warning with recipient/subject/preview instead of sending, so signup itself is unaffected). Provider: **Resend** (no-card free tier, matches the standing anti-billing-signup pattern). New migration `010_email_verification.sql` adds `users.email_verified BOOLEAN NOT NULL DEFAULT false` (existing accounts backfilled `true` — only new signups must verify) + `email_verification_tokens` (hashed token, mirrors `sessions.token_hash`'s SHA-256 pattern in `src/lib/auth.ts`, 48h expiry, single-use via `used_at`). **Applied to the live Neon dev DB** and verified column/table exist. `src/lib/email.ts` = the one wrapper module for all outbound Resend calls (lazy-imports the SDK, 8s timeout via `Promise.race`, never throws — logs loudly and returns `{sent:false}` on any failure so a Resend outage or missing key can never break signup). `src/lib/email-verification.ts` generates/hashes/validates tokens (`SELECT ... FOR UPDATE` inside a transaction for atomic single-use consumption, same defense-in-depth class as the advisory-lock rate limiter). Signup route (`/api/auth/signup`) now creates a token and sends one combined welcome+confirmation email (subject "Confirm your Racoon Eye account") with a light IHN mention (full IHN-sharing detail stays #19's job) — wrapped in try/catch so a token/email failure never fails the 201 response. New `GET /api/auth/verify-email?token=` (public, rate-limited 20/hr/IP, audit-logs every outcome as new `email_verified` action) redirects to a new `/verify-email` page (success/expired/used/invalid/rate_limited states, reuses `Card`/`.auth-page`/`Button` styles). **Decision: non-blocking** — unverified accounts are NOT locked out; dashboard shows a persistent "please verify your email" banner (via `/api/biodata/me` now also returning `email_verified`) instead, per the plan's recommended default (no existing precedent in this codebase for hard-blocking partial accounts). Existing signup IP rate limit (15/hr, already in place for bcrypt-CPU-DoS) reasonably bounds the email-send volume too — no separate email-specific limit was needed there (the new verify-email endpoint has its own, separate bucket). Tests: token generation/hashing/expiry/single-use (`email-verification.test.ts`), the Resend wrapper's key-present/absent/error/throw paths (`email.test.ts`), signup calling the email wrapper and surviving a throw from it or from token creation (`auth-signup.test.ts`), and the verify-email route's success/expired/used/invalid/rate-limited paths (`verify-email.test.ts`). 375/377 tests green (`--runInBand`; the 2 failures are in `hospital-filters.test.ts`, unrelated to this change — confirmed via `git status` that another concurrent session was mid-edit on that file for a different worklist item), `tsc --noEmit` clean, `next lint` clean. |
| 19 | Forgot-password flow, secure + functional; requires email confirmation; warn users about their unique IHN string at signup and in the welcome email | Done — new migration `012_password_reset_tokens.sql` (+`.down`, applied to live Neon dev DB and column-verified) adds `password_reset_tokens` (hashed token, `user_id` FK cascade, `expires_at`, `used_at`, `created_at` — same shape as `email_verification_tokens`) with a deliberately **short 45-minute expiry** (vs. email verification's 48h) since a leaked/guessed reset token is a full account takeover, not just a verified-flag flip. `src/lib/password-reset.ts` mirrors `email-verification.ts`'s exact security posture: crypto-random 32-byte token, only its SHA-256 hash stored, `SELECT ... FOR UPDATE` inside a transaction for atomic single-use consumption, expiry compared in SQL. On successful consumption it validates the new password with the same `validatePasswordStrength` used everywhere else (not forked), hashes it via the existing `hashPassword` helper, updates `users.password_hash`, marks the token used, and **deletes every session for that user** (unlike `PATCH /api/account/password`'s "keep my current session" behavior, there is no session to preserve here since the request itself is unauthenticated — the whole point is to force re-auth everywhere). New public `POST /api/auth/forgot-password`: **enumeration-safe** — every branch (registered email, unregistered email, invalid-format email, rate-limited) returns the exact same generic `{message}` body; a syntactically invalid email short-circuits before any DB call (that only reveals "not email-shaped", not "not registered"), while a valid-format miss still runs the same `findUserByEmail` lookup a hit does. Documented, accepted tradeoff: the one part not fully timing-equalized is that a genuine hit also creates a token row and calls the outbound email provider (added network latency), while a miss does not — closing that fully would mean sending a real email on every miss too (worse: spam/cost for a low-value side channel), so it was a deliberate judgment call, not an oversight. Rate-limited on **two independent dimensions** (`forgot_password_ip:<ip>` 5/hr and `forgot_password_email:<email>` 3/hr) via the existing `checkRateLimit`, since this is a prime email-bombing target; the throttled path returns the identical generic response (not a distinct 429) and is still audit-logged as `rate_limited` (the exact gap #18's security audit flagged elsewhere). New public `POST /api/auth/reset-password` accepts `{token, newPassword}`, calls `consumePasswordResetToken`, and audit-logs every outcome (`password_reset_requested` on the forgot-password call with a `found: true/false` detail visible only in the audit trail, never the response; `password_reset_completed` with `success`/`reason` on the reset call). **IHN-warning interpretation (ambiguous in the item text, judgment call made and documented here):** "in the welcome email" — added a brief line to the existing welcome/confirmation template (`buildWelcomeVerificationEmail`) reminding the reader the IHN code is a static, never-changing emergency key to keep private, consistent with (not contradicting) `IHNCodeDisplay.tsx`'s existing "why does this matter" copy. "Upon signing in" — interpreted as a **one-time dashboard banner shown after login** (not a hard gate, and not the login *page* itself, since the login form has no user context yet to know whose IHN to reference) — mirrors the #18 email-verification nudge-banner pattern in `DashboardClient.tsx`, tracked via a per-user `localStorage` flag (dismissible "Got it" button) rather than new schema, since this is a UX nudge, not an access-control decision. UI: `LoginForm.tsx` gained a "Forgot password?" link (login tab only) to a new `/forgot-password` page (`ForgotPasswordForm.tsx`, generic confirmation regardless of outcome) and a new `/reset-password` page (`ResetPasswordForm.tsx`, token read from the URL query param). Extracted the live password-strength checklist from `LoginForm.tsx` into a new shared `PasswordStrengthMeter.tsx` component (reused by both signup and reset-password, per the "don't duplicate the checklist markup" instruction) instead of forking the markup. Tests: `password-reset.test.ts` (token gen/hash/expiry/single-use/session-kill, mirroring `email-verification.test.ts`), `forgot-password.test.ts` (same-response enumeration-safety assertion, rate-limit-on-both-dimensions, no token/email work on a miss or inactive account, survives an email-send throw), `reset-password.test.ts` (valid/expired/used/invalid/weak-password/rate-limited paths + audit assertions), plus component tests for `ForgotPasswordForm`/`ResetPasswordForm` and a `LoginForm` link-visibility test. **398/398 tests green** (`--runInBand`), `tsc --noEmit` clean, `next lint` clean. Migration applied + column-verified against the live Neon dev DB. |
| 20 | Rename "Biodata Farm" → "Biodata" everywhere | Done — dashboard heading + `docs/pages/dashboard.md` updated; no test asserted on the old string |
| 21 | No arrangement for a profile photo | Done — added `profile_photo_url?: string` to `ProfileLayer`; reused the existing `uploadFile`/`/api/uploads` plumbing (no new upload route) with a minimal single-image control in `BioDataForm.tsx`'s Profile section (circular preview, upload/remove, frosted-glass row matching `Card.module.css` tokens); both `biodata/me` and `biodata/[userId]` PATCH routes re-validate the URL with `safeHttpUrl` (sanitizeLayer only escapes HTML, doesn't check scheme) so a `javascript:`/`data:` value is dropped instead of stored. Verified round-trip (save → reload) via new route + component tests. **Clarified (same day):** "Remove photo" is deliberately display-only — founder confirmed server data should never be deleted by this action, only what's shown on the frontend changes. A code-review pass had flagged the opposite (that removal silently failed to clear the server-side value) and a fix was applied to make it actually clear on the server; reverted per founder direction, and the regression test now asserts the intended behavior (outgoing payload omits the key; server value stays untouched). |
| 22 | Add a dropdown explanation of why [biodata/IHN] is important | Done — collapsible "Why does this matter?" added to `IHNCodeDisplay`, frosted-glass treatment matching `Card.module.css`'s glass tokens |
| 23 | Users should be able to select what is visible through the IHN link (granular field-level sharing controls) | Done — closes the long-standing "IHN sharing is a dead path" gap. `authorize()` in `GET /api/biodata/[userId]` no longer requires `session.user_id === paramUserId`; a DIFFERENT authenticated patient session may now read the target's row if they supply the correct IHN code (`constantTimeEquals`, reused not reimplemented), but the response is filtered server-side through a new default-deny `sharing_prefs` JSONB column (migration `013_biodata_sharing_prefs`, applied to live Neon) via a pure `filterBiodataBySharingPrefs` helper (`src/lib/sharing-prefs.ts`) so #24/#25 can reuse it unchanged. **Granularity decision:** grouped by logical section (9 keys — `profile`, `demographics`, `anthropometrics`, `chronic_disease`, `genotype`, `blood_group`, `disability`, `health_preferences`, `clinical_conditions`) rather than one flag per individual field — covers every `BiodataLayer`/`ProfileLayer` field that exists today so nothing can leak through an uncovered gap, while keeping the dashboard toggle UI to ~9 switches. PATCH (write) stays strictly owner-only, unchanged. **Rate-limit design:** cross-user reads get their OWN two buckets, distinct from the existing owner bucket (`biodata:<userId>`) — `biodata_shared_target:<targetUserId>` (10/hr, bounds brute-forcing ONE victim's code, the real threat) and `biodata_shared_ip:<ip>` (30/hr, bounds one attacker spraying guesses across many targets), mirroring the dual-bucket pattern already used by forgot-password. New `biodata_shared_read` audit action distinguishes a cross-user read from the owner's own `biodata_read`, logs the requester, target, and the ACTUAL field names returned (not just "some") — logged on both success AND IHN-mismatch failure (the abuse signal). New `SharingPrefsPanel` component on the dashboard (reuses the `show_doctors`/`HospitalFilters` checkbox-row pattern, no new UI language) lets the owner toggle each section; `PATCH /api/biodata/me` now also accepts a `sharing_prefs` full-replace payload. Tests: `sharing-prefs.test.ts` (pure-function default-deny + field-level isolation — a field NOT opted in is never returned even when every other field is opted in), new cross-user-read describe block in `biodata.test.ts` (dead-path-fixed, field-level isolation via the real route, default-deny-when-empty, distinct audit action on success AND failure, distinct rate-limit buckets, 429), and an owner-unaffected-by-prefs case in `biodata-me.test.ts`. **414/414 tests green** (`--runInBand`), `tsc --noEmit` clean, `next lint` clean. Migration applied + column verified against the live Neon dev DB (`jsonb`, default `'{}'`). Not yet touched: #24/#25 (public "string tab"/QR lookup) — explicitly out of scope for this task, queued next to consume `filterBiodataBySharingPrefs`. **Dedicated adversarial security-auditor pass completed (same day)**: no critical/high findings — default-deny confirmed genuinely unbypassable, IHN comparison correctly constant-time, PATCH confirmed still owner-only, `ihn_code` confirmed stripped by allowlist (not omission-by-luck), field-level isolation test confirmed real. Two low findings fixed: the `profile` sharing toggle was bypassing the pre-existing `dob_visible` opt-out (now respected), and rate-limited cross-user attempts weren't audit-logged (now logged, same signal class as IHN-mismatch attempts). |
| 24 | Homepage "string" tab: lets people look up a user's IHN string for biodata extraction, output as an organized document; must be extensively logged, dev-accessible | Done — new nav entry "Find by IHN" (`Header.tsx`, both inline nav ≥1180px and hamburger menu) → dedicated `/string-lookup` page (`StringLookupClient.tsx`), kept OFF the full-bleed map homepage per the item's own reasoning. **Public and opt-in-only, consuming #23's sharing-prefs model exactly as-is** — not a dev-only bypass; a second, unfiltered lookup path was explicitly out of scope. Requires an authenticated patient session (same gate #23's cross-user IHN read already requires — this page doesn't loosen that). **Lookup-key decision:** `biodata.ihn_code` is `UNIQUE` (migration 001), so a code alone is a sufficient key — a person in an emergency has the code, not the target's internal UUID. New `GET /api/biodata/lookup?ihn=CODE` resolves code→user_id, then hands off to the SAME `authorizeRead`/`buildReadResult` pipeline `[userId]/route.ts` uses (both now exported from that file so nothing is reimplemented). Pre-resolution gets its OWN sibling rate-limit buckets (`biodata_lookup_code`/`biodata_lookup_ip`, mirroring `biodata_shared_target`/`biodata_shared_ip`'s 10/hr+30/hr limits) since the user id isn't known yet; post-resolution reuses the EXACT existing `biodata_shared_target`/`biodata_shared_ip` buckets. Every outcome is audit-logged: a code matching no one at all logs a new `biodata_lookup_no_match` action (dev-visible only); the response body is enumeration-safe — "no such code" and "code exists but nothing opted in" both collapse to the identical `{available:false}` shape, same principle as #19's forgot-password. Output renders as an organized document (`src/lib/biodata-document.ts`'s pure `buildBiodataDocument`, grouped into Profile/Demographics/Anthropometrics/Chronic disease/Genotype/Blood group/Disability/Health preferences/Clinical conditions, a section omitted entirely when empty) via `Card`, with a `window.print()` button and print-friendly CSS — no PDF export (out of scope for v1, noted here). Tests: `biodata-document.test.ts` (pure section-building), `biodata-lookup.test.ts` (session gate, malformed code, both rate-limit buckets, no-match vs. nothing-shared enumeration-safety, matched-with-sharing via the same pipeline, bucket reuse assertion), `StringLookupClient.test.tsx` (sign-in gate, client-side format validation, no-match rendering, document rendering, QR prefill, server error surfacing). **Chained security-auditor pass completed (same day):** no critical/high findings — confirmed the lookup route genuinely reuses `authorizeRead`/`buildReadResult` (no parallel/weaker auth path), the by-code resolution is exact-match parameterized SQL (no injection/fuzzy match), the server-side session gate can't be bypassed by a direct API call, enumeration-safety response shape confirmed byte-identical across no-match/nothing-shared/rate-limited branches, XSS clean (no `dangerouslySetInnerHTML`), QR-in-URL exposure assessed as acceptable given the code is an explicitly shareable non-rotating credential. One low/accepted finding: match vs. no-match branches take measurably different DB round-trip counts (~3-4x), a narrow timing side-channel bounded by the existing rate limits and the code's ~10^18 keyspace — not fixed, documented as an accepted low-severity tradeoff rather than left silent. |
| 25 | QR code accessible on the string tab | Done — client-side only via the `qrcode` npm package (no new backend surface). **Placement decision:** the QR is generated in `IHNCodeDisplay.tsx` (dashboard, where the account holder already sees their own code) rather than only on the lookup page itself, since the primary use case is the account holder handing their phone to someone else to scan — encodes a URL to `/string-lookup?ihn=<code>` (not the raw code text) so scanning lands directly on a working, pre-filled lookup form instead of requiring the scanner to navigate to the site and retype the code. `StringLookupClient.tsx` reads that `?ihn=` query param on mount and pre-fills the input, completing the round trip — this is the "room for a QR code on the string tab" the item asks for: the tab is the landing point the QR deep-links into. |
| 26 | Footer help bar: detailed instructions organized by function/procedure, with its own keyword search | Done — added a "Help" link in `Footer.tsx` that opens the existing `Modal.tsx` as a help panel (`HelpBar.tsx`); content is static structured data (`src/lib/help-content.ts`, `{section, topic, answer}[]`) covering search modes, IHN code, Biodata, First Aid, and suggestions; client-side substring search filters across all three fields; styled with the existing glass tokens (`Card.module.css` pattern). Chose a modal over a `/help` page since Modal already provides Escape/focus handling and the content fits comfortably without full-page navigation. New test `HelpBar.test.tsx`. |
| 27 | Religion, tribe, ethnicity should be optional | Done — confirmed `religious_status` was already optional (not in `BioDataForm` validation); added optional `tribe`/`ethnicity` to `BiodataLayer` + form inputs; `sanitizeLayer` is a generic string sanitizer (no fixed allowlist) so no save-path change needed |
| 28 | Chronic disease section needs slots: disease, duration, progression, complication, care, cause (recommend doctor aid) | Done — extended `ClinicalCondition` with `duration?`/`progression?`/`complication?`/`care?` (disease=`condition`, cause already existed); added the four optional inputs to the add-condition sub-form in `BioDataForm.tsx` plus a "recommend doctor aid" helper note (copy only, no workflow); **critically whitelisted the 4 new keys in `sanitizeClinicalConditions` (`src/lib/sanitize.ts`)** — this is the exact silent-drop bug class flagged from the 2026-07-12 `cause`/`timestamp` addition, verified via a save/reload round-trip test in `biodata-me.test.ts`, not just rendering. |
| 29 | Build a system to generate a report from IHN string/QR data — neatly organized, photo top-right, declaration + doctor signature block (name/contact) where doctor-recommended fields were approved | Open — large feature, depends on #23/#24/#30 |
| 30 | Doctor must explicitly consent to use of their name/info; doctor must be contacted and consent verified before approval; space to record denial of consent | Open — large feature, compliance-sensitive |
| 31 | Bolder/thicker/sharper-edged font | Done — base body weight 400→500, headings 700/800 with tightened letter-spacing (`globals.css`); same system-ui stack, no typeface swap; checked Button/Card/Header, no overflow/contrast regressions (component-level font-weight overrides take precedence) |
| 32 | Some static info feels "base"/out of place — needs a content/design pass | Open — needs founder to point at specifics |
| 33 | First aid entries don't feel intuitive; no space for pictures; keep simple but make intuitive | Done — **images**: `FirstAidForm.tsx`'s image field was a hidden raw newline-URL textarea with a single bolted-on file-upload button appending into it; replaced with a real multi-image control (file upload via the existing `uploadFile`/`/api/uploads` plumbing + URL-paste fallback, thumbnail grid with per-image remove, capped at 10 to match the existing server-side cap) — same visual language as `PhotoUpload.tsx`'s grid/remove-button pattern, not a new one. **Intuitiveness**: `FirstAidDetail.tsx` was one flat `<dl>` of 9 fields with no hierarchy; regrouped into headed sections (Overview → Signs & symptoms → Process → Do's & Don'ts → Additional guidance — the order #34 itself suggests), each only rendering when it actually has content (an empty "Process" heading would be more confusing, not less). Also fixed a real pre-existing gap while touching this file: `entry.tags` badges referenced `styles.tags`/`styles.tag` classes that were never defined in `FirstAidDetail.module.css` (dead/invisible styling) — added them, and a visually distinct `.symptomTag` style for the new signs/symptoms chips so the two whitelist-driven axes (topic tags vs. signs/symptoms) read as different concepts. Public catalog's existing read-only structure, 13-tag filter UI, and disclaimer left untouched — polish pass, not a rebuild. |
| 34 | Add signs/symptoms section before procedure in first-aid entries (so people can tell if indicated); symptom search bar should require min. 2 symptoms to narrow to the right entry | Done — **schema**: new migration `011_first_aid_signs_symptoms` (+ `.down`) adds `first_aid_entries.signs_symptoms TEXT[] NOT NULL DEFAULT '{}'` + a GIN index, mirroring migration 005's `tags` column exactly. Deliberately a genuinely SEPARATE column reusing the SAME whitelist as `tags` (`first-aid-tags.ts`), not a repurposing of `tags` itself — `tags` is the topic/scenario axis used for catalog browsing/filtering (e.g. "show me every Bleeding entry"), `signs_symptoms` is per-entry content describing what a reader should actually notice to tell this entry applies, which won't always equal the filing tag(s); full reasoning documented in the migration file. Applied to live Neon. **Display + form**: `FirstAidForm.tsx`'s signs/symptoms chip-picker (same UI pattern as the existing topic-tag picker) now sits between Description and Process in both field order and the underlying form layout — the item's literal ask; `FirstAidDetail.tsx` renders it as its own "Signs & symptoms" section before "Process" (see #33's grouping). Create/PATCH routes persist it via the same `normalizeTags` whitelist normalizer as `tags`; public catalog GET now selects it. **Min-2-symptom search**: `Header.tsx`'s symptom search mode converted from free text to a proper whitelist multi-select (reusing the same chip-picker pattern as First Aid's own tag picker and #13's `HospitalFilters.tsx`, rather than inventing a third "pick from whitelist" UI) — a popover anchored under the search pill (needed its own wrapper since the pill itself clips overflow), showing a live "N/2 minimum selected" hint; the Search button is disabled and submit is a no-op below 2 selections. This feeds directly into #8's hospital ranking (same `FIRST_AID_TAGS` whitelist, mapped via `symptom-specialty-map.ts`). Tests: round-trip coverage for `signs_symptoms` through create/PATCH (`dev-first-aid.test.ts`), the public list SELECT (`first-aid-entries.test.ts`), and a new `Header.test.tsx` covering the picker/disabled-button/no-navigate-below-2/navigates-at-2 cases. 366/366 tests green, `tsc --noEmit` + lint clean. |

## Standing reminders owed to founder
- **#9** — need the Linktree URL for footer links.
- **#15** — need the new logo asset.

## Direct question to answer now
- **#16** — panda eye vs raccoon eye (see chat response): raccoon eye, no contest — stronger/more distinctive brand hook, already built.

## Phased execution plan
See [PLAN-worklist.md](PLAN-worklist.md) for the full phased plan (planner pass, 2026-07-21) — recon findings, phase groupings, parallelization, and open decisions. Update this file's Status column as items land; keep the raw list above unedited.
