import DevInstitutionsClient from './DevInstitutionsClient';

export const metadata = { title: 'Institution accounts', robots: { index: false, follow: false } };

export default function DevInstitutionsPage() {
  return <DevInstitutionsClient />;
}
