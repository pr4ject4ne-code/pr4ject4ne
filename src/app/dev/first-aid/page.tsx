import DevFirstAidClient from './DevFirstAidClient';

export const metadata = { title: 'Dev · First Aid', robots: { index: false, follow: false } };

export default function DevFirstAidPage() {
  return <DevFirstAidClient />;
}
